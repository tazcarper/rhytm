# Rhythm Outdoors — Project Tracker

Status key: ✅ Done · 🔄 In Progress · ⏸ Blocked · 🔲 Not Started

---

## Session Handoff (last updated 2026-05-18)

**Where we are.** Foundation, auth gate, Phase 7 RLS reference doc, and the production `/login` surface (magic-link + Google OAuth) are all landed and partially verified against live Supabase. The JWT-refresh path in the callback was hardened during testing — Google sign-in for both admin and member is now confirmed working end-to-end.

- **Database** — All 9 migrations applied to project `vgmlordqsigalrpmuwap` (Phases 1, 2, 3, 4, 5, 6 + Phase 4 hotfix + split-into-people-memberships refactor + household visibility). Phase 7 is documentation-only and regenerated at `plan/supabase/phase-7-rls.md` — canonical RLS reference matching the live schema.
- **Schema model** — `people` + `memberships` + `membership_people` junction (supports cross-property + household sharing). RLS uses six SECURITY DEFINER helper functions (`current_person_id`, `current_member_membership_ids`, `current_member_active_membership_ids`, `current_member_active_property_ids`, `current_household_person_ids`, `staff_visible_person_ids`) to break cross-table policy cycles — see CLAUDE.md "RLS Rules" and Phase 7 doc for the patterns.
- **Auth gate** — All 5 scenarios in `docs/manual-testing.md` (A, B, B2, C, D, E) passed end-to-end on 2026-05-18 against live Supabase. Scenario F (production `/login`) added the same day; partially verified live (Google OAuth admin + member sign-in confirmed working after a JWT-refresh fix, see below) — full systematic Scenario F run still pending.
- **`/login` surface** — Real production page at `/login`, styled per the HSB members portal reference (umbrella Rhythm Outdoors brand). Server component with `?next=` open-redirect guard + already-signed-in redirect. Client `<LoginForm>` offers magic-link (`signInWithOtp({ shouldCreateUser: false })`) AND Continue-with-Google (`signInWithOAuth({ provider: 'google' })`). `<CyclingProperty>` beneath the wordmark fades through Horseshoe Bay / Hog Heaven / Packsaddle. `portalHomeForRole()` extracted to `lib/auth/portal.ts` and shared with `/auth/callback`. Failed sign-ins (no pending invite / expired invite) now render as a dismissible inline `<LoginAlert>` at the top of the card via `/login?error=invite-not-found&email=...`; the standalone `/invite-not-found` route was removed.
- **Design system** — `app/globals.css` now carries a full token system, not just the brand palette: semantic accents (error/warn/info/success), elevation shadows (soft/lift), serif + sans family aliases, a full type scale (`--text-eyebrow` 11px → `--text-display` clamp(48,8vw,72)), tracking + leading scales, 4px-base spacing (`--space-1`…`--space-24`), radius scale (sharp/card/pill), motion timings (fast/base/slow), layout widths (max/narrow/prose), AND a global `prefers-reduced-motion: reduce` baseline that clamps animations to 0.01ms. Cormorant Garamond + Inter loaded via `next/font/google` in `app/layout.tsx`.
- **Callback hardening** — `/auth/callback` now calls `supabase.auth.refreshSession()` unconditionally before redirecting (previously only in the first-time-link branch). Fixes a real bug where Google OAuth landed users on `/unauthorized` even though their `auth.users.app_metadata.role` was correct — the post-exchange JWT was missing the role claim, and the role check in middleware bounced them.
- **Supabase dashboard config** — Google OAuth provider is configured (Google Cloud OAuth client + Supabase Auth → Providers → Google enabled). Account linking is on, so duplicate auth users from email + Google identity collisions should no longer happen.
- **Application** — Next.js 16 + TypeScript scaffold; three Supabase clients (browser publishable, cookie-aware server, secret-key service role); auth middleware with strict role-per-portal allowlist; `/auth/callback` with PKCE + token_hash dual-flow support and unconditional JWT refresh; `/member` stub with household-member visibility; production `/login` (per above); `/admin`, `/partner`, `/unauthorized`, `/auth/auth-code-error` stubs; `/dev` test dashboard. `tsc --noEmit` clean.
- **Vercel deploy** — not yet connected. Effectively the only operational gap left on App 1.
- **Open client questions** — Q2 / Q4 / Q5 / Q9 / Q14 / Q16 still block seed data for `time_slots`, `services`, `add_ons`, `instructors`, `pricing_rules`, and the membership tier vocabulary. None block schema or auth work — all that is done.

**How to resume in a fresh session:**

1. `git pull` to get to the latest main.
2. Read `CLAUDE.md`, then this file, then the plan doc for whichever phase you're touching next (in `plan/supabase/phase-*.md`).
3. Verify the DB hasn't drifted: `./node_modules/.bin/supabase migration list` should show 9 matched rows.
4. Verify local dev: `npm run dev` should boot and list 3 properties at `/`.
5. Pick one of the next moves below and go.

**Dev tool — `/dev`.** A temporary password-gated dashboard at `http://localhost:3000/dev` for testing the auth flow: create person + membership(s) (multi-property checkbox-select), add authorized person to existing membership (household scenario), send invite, generate magic-link URL (no email — bypasses rate limit + template config), force-expire invite, stamp `app_metadata.role` on any user, reset a test user (deletes person + memberships where primary + auth user). Gate uses the `DEV_DASHBOARD_PASSWORD` env var (server-only — not `NEXT_PUBLIC_*`). Set it in `.env.local` before first use. Cookie is `httpOnly`, scoped to `/dev`, 24-hour TTL; rotates automatically when the env var changes. **Remove the entire `/dev` tree before launch.**

**Manual test plans live in `docs/manual-testing.md`.** Seven scenarios for the auth gate: A (single-property), B (cross-property), B2 (household sharing), C (wrong-role bounce), D (expired invite), E (property-manager scope), F (real `/login` page — 10 steps including magic-link and Google OAuth paths). A–E all passed end-to-end on 2026-05-18 — see Verification log in the testing doc. F is partially verified live (Google OAuth admin + member confirmed working after the JWT-refresh fix; invite-not-found alert verified). Re-run A–F before any change touching `/auth/callback`, `middleware.ts`, the people/memberships/junction schema, the `/login` page, or any RLS policy on those tables.

**Next moves — pick one based on what the user wants:**

- **A. Continue App 4 — my bookings + adventures listing on `/member`** *(recommended)*. Replace the household-visibility stub with the real member dashboard. Lists the member's bookings (via `bookings.member_user_id = auth.uid()` — RLS already in place) and adventures available at any of their active properties (via `current_member_active_property_ids()` — already in place). RSVP UX comes after.
- **B. Configure custom SMTP (Resend) for Supabase Auth** — replaces the built-in mailer's ~3–4/hour rate limit and unblocks the magic-link email path on `/login` for production deliverability. ~30 min in the Supabase dashboard. Resend is already in the project stack. After this, Scenario F steps 2 + 3 + 5 can be run with real email instead of the `/dev` substitute.
- **C. Wire Vercel deploy** — Connect the GitHub repo in the Vercel dashboard, add the same env vars from `.env.local` (publishable key, secret key, `BID_COOKIE_SECRET`, `DEV_DASHBOARD_PASSWORD`), trigger the first deploy. ~30–60 min, no code. Worth doing soon so preview URLs work for client review.
- **D. Finish the formal Scenario F pass** — most of the scenario was hit during live testing this session (Google OAuth admin + member, invite-not-found alert) and exposed + fixed the JWT-refresh bug. A clean re-run against the current code is still worth doing — particularly steps 2/3/5 (magic-link email round-trip, `?next=` passthrough) if Path B (SMTP) is done first, otherwise via `/dev` magic-link substitute. ~15 min.
- **E. Start App 2 (Public Booking Flow)** — property selection → service → time → guest info → checkout → bid page. All DB tables it touches exist, though `services`/`time_slots`/`pricing_rules` rows are blocked on client questions. Could be built against placeholder seed data.
- **F. Get the six open client questions in front of the client.** Q2 + Q4 are the lowest-cost asks ("send us your operating hours" + "send us your catalogs") and unblock the most seed data. Pure project management — no code work for us.

**Recommendation:** **A (member dashboard)** is the natural next build now that login is in. **B (Resend SMTP)** is the lightest dashboard-only task and unblocks real magic-link delivery — do it in parallel if you can. **C (Vercel)** is the same shape — dashboard config, no code — and lets the client see preview URLs.

---

## Database Phases (Supabase)

| # | Epic | Status | Completed | Notes |
|---|------|--------|-----------|-------|
| Phase 1 | Foundation Tables | ✅ Done | 2026-05-17 | properties (3 seeded), time_slots, services, add_ons, service_add_ons, instructors, pricing_rules |
| Phase 2 | Booking System | ✅ Done | 2026-05-17 | bookings, booking_disciplines, booking_add_ons. Four ordered BEFORE triggers (00_compute_end_time, 01_set_capacity_reserved, 02_validate_start_time, 03_check_property_capacity) + updated_at + deferred booking_add_ons_check_discipline constraint trigger. Instructor exclusion constraint using tstzrange. UNIQUE partial indexes on both Stripe payment intent columns. Post-review fixes (trigger ordering, discipline check, payment intent uniqueness) folded directly into the Phase 2 migration. RLS perf wrapping `auth.jwt()` / `auth.uid()` in `(SELECT …)` already in place. |
| Phase 3 | Bids | ✅ Done | 2026-05-18 | `bids` (with `access_code_hash`), slug generation, `validate_bid_access_code()` SECURITY DEFINER function, bid↔booking sync trigger that RAISEs on unexpected source states, polling-based status updates (Realtime intentionally not used — anon clients can't receive RLS-gated events), `UNIQUE` partial indexes on `dropbox_sign_envelope_id` and `refund_payment_intent_id`. Requires `pgcrypto` extension. |
| Phase 4 | Auth & Users | ✅ Done | 2026-05-18 | `partner_organizations` (split admin insert/update/delete policies); `people` + `memberships` + `membership_people` junction (after the 2026-05-18 split — see refactor row below); `membership_status_enum`. Five JWT helper functions: `auth_role`, `auth_property_id`, `auth_partner_org_id`, `is_admin`, `is_staff` — all `SECURITY INVOKER`, all wrap `auth.jwt()` in `(SELECT …)`. Application-side pieces (`/auth/callback` person link + invite expiry enforcement, Inngest seed-people-invites) are App-phase work, not DB. Hotfix `20260518230913_phase_4_fix_user_id_unique.sql` dropped accidental UNIQUE on the old `members.user_id` (since superseded by the split). |
| Phase 4 refactor | members → people + memberships | ✅ Done | 2026-05-18 | Migration `20260518232029_split_members_into_people_memberships.sql`. Splits the old `members` table into `people` (the human — email, name, phone, auth link), `memberships` (the account — member_number, tier, dues, status, property), and `membership_people` (junction with role: primary/spouse/dependent/authorized). Partial unique index enforces exactly one active primary per membership. Triggers `ON DELETE CASCADE` from memberships to junction, and from people to junction. RSVPs recreated against `memberships.id` (with `created_by_person_id` for audit) — UNIQUE (adventure_id, membership_id) means one RSVP per membership per adventure, regardless of which spouse made it. `member_adventures` member-read RLS updated to traverse people → membership_people → memberships. Resolves the "two emails sharing a membership" product question. |
| Phase 5 | Member Adventures | ✅ Done | 2026-05-18 | `member_adventures`, `member_adventure_rsvps`. Capacity trigger with `FOR UPDATE` row lock enforces total capacity + per-RSVP `max_guests_per_rsvp` cap + staff-set `is_manually_sold_out` block (3rd-party operator says full). Pricing: solo `price` + nullable `guest_price` add-on per extra guest. Auto-sync `sold_out` triggers skip when `is_manually_sold_out=true` so a single cancellation can't re-open booking. Member RLS joins `members` on `user_id = auth.uid()` for cross-property reads; member writes route through Server Actions (no `FOR UPDATE` policy). Waitlist via Inngest (depends on Phase 6 webhook layer); Inngest waitlist promoter re-reads the manual flag and aborts if set. |
| Phase 6 | Webhook Idempotency | ✅ Done | 2026-05-18 | `processed_webhooks` with `(id, source, event_type, payload)`, PK `(id, source, event_type)` for multi-event-per-object providers. Claim-first idempotency pattern via `INSERT … ON CONFLICT DO NOTHING`. RLS enabled with no policies (defends against Supabase's default GRANTs to anon). `pg_cron` weekly cleanup at 30 days. |
| Phase 7 | RLS Architecture | ✅ Done | 2026-05-18 | **Documentation only — no migration.** Canonical RLS reference regenerated to match the live schema: people/memberships/junction model, six SECURITY DEFINER helper functions, three RLS cycles encountered and how each was fixed, complete policy reference per table, strict portal allowlist + preview-as-member design, testing protocol (refs `docs/manual-testing.md`), common pitfalls, and a changelog of every policy-level change since Phase 4 cut. Maintenance rule: update in the same commit as any RLS or helper change in Phases 1–6. See `plan/supabase/phase-7-rls.md`. |

### Pending Seeds (blocked by client confirmation)

| Table | Blocked By |
|-------|-----------|
| `time_slots` | Q2 — operating hours per property |
| `services` | Q4 — full discipline catalog (HSB partial ready) |
| `add_ons` | Q4 |
| `service_add_ons` | Q4 |
| `instructors` | Q2 — headcount per property |
| `pricing_rules` | Q5 — pricing formula confirmed ($200/hr private lesson done; group tiers TBD) |

---

## Application Phases (Next.js)

| # | Epic | Status | Notes |
|---|------|--------|-------|
| App 1 | Project Scaffold | ✅ Done | Completed 2026-05-19. Eleven sub-phases (scaffold → Supabase clients → design tokens → UI primitives → auth middleware → callback → `/login` → portal stubs → `/dev` dashboard → manual test pack → Vercel deploy). Full breakdown + goals per sub-phase in `plan/app/app-1-scaffold.md`. Auth gate verified live (Scenarios A–E passed 2026-05-18, Scenario F Google-OAuth path verified during JWT-refresh-fix testing). Vercel deploy connected with env vars mirrored from `.env.local`. App 2 unblocked. |
| App 2 | Public Booking Flow | 🔄 In Progress | **Sub-phase 2.1 landed 2026-05-19:** public route group at `app/(public)/` (proxy.ts does not gate it), minimal public header (logo only), property picker at `/book` reading the `properties` table via `src/services/public/properties.ts`, per-property layout at `/book/[property]` mounting `<BookingFlowProvider>` (client-side React Context — funnel state survives back-nav, refresh resets), `<BookingFlowGuard>` for steps 2+ that redirects to `/book/[property]?reset=1` and renders a "let's start over" alert. No cookies / localStorage / URL state beyond the `[property]` slug. **Post-land review passes (same day):** `/simplify` (three parallel agents — reuse / quality / efficiency) and a manual walkthrough of `.agents/skills/vercel-react-best-practices` produced cleanups in-place — dropped a duplicate property fetch in the layout, dropped `propertySlug` from the provider (read from `useParams()`), dropped a no-op `useMemo`, hoisted `?reset=1` into a `BOOKING_RESET_PARAM` / `buildBookingResetUrl()` contract, derived `BookingFlowGuard.hasAll` during render instead of via `useState` + effect, promoted `propertyOrdinal()` + `getPublicProperties()` so `app/page.tsx` consumes the new shared helpers, and slashed WHAT-comments across the new files. Editorial-distinct taglines on the home page were kept (reviewer over-claimed identity). **Sub-phase 2.2 landed 2026-05-19:** `<BookingTypePicker>` at `src/components/public/booking-flow/booking-type-picker.tsx` — three branded cards (Plan a Visit / Private Lesson / Host an Occasion) reading from `BOOKING_TYPE_META: Record<BookingType, BookingTypeMeta>` in `src/constants/public/booking-types.ts`. Click writes `bookingType` to context + `router.push` to `/disciplines`; prior selection highlighted via `data-selected` on back-nav. Host-an-Occasion shows the exclusive-use notice copy inline on its card. `app/(public)/book/[property]/page.tsx` now renders the picker (replacing the 2.1 placeholder) with a "What kind of visit are you planning?" eyebrow prompt. Disciplines step placeholder at `app/(public)/book/[property]/disciplines/` — server page wraps `<BookingFlowGuard requires={['bookingType']}>` around a client `<DisciplinesPlaceholder>` that reads context and shows the chosen type's title + duration. New shared `<StepPage>` + `<StepPageHead>` at `src/components/public/booking-flow/step-page.tsx` consumed by both step pages — collapses duplicated `.shell` + `.head` styles ready for 2.3–2.5. **Post-land review (/simplify, three parallel agents):** dropped unused `shortLabel` from `BookingTypeMeta`, converted `bookingTypeMeta()` lookup to direct `BOOKING_TYPE_META[type]` (O(1) + clearer intent, removes a defensive throw), dropped `force-dynamic` from the placeholder disciplines page (no data fetching), extracted shared step-page chrome (above). Skipped with reasoning: useMemo on provider value (setState/reset already `useCallback`'d — memo would be a no-op), `radiogroup` ARIA (cards are click-to-advance, not separate-commit radios — `aria-pressed` reads correctly), Button primitive swap (would diverge from `property-card`'s raw-Link pattern). **Sub-phase 2.3 landed 2026-05-20:** `<DisciplinePicker>` at `src/components/public/booking-flow/discipline-picker.tsx` — vertical list of expandable service cards. Click selects + reveals nested add-ons inline; add-ons toggle on/off with a +/− quantity stepper (clamped 1–20). Single-select for `private_lesson`, multi-select for `plan_a_visit`. All edits write straight to `BookingFlowProvider` context (no local state). Continue button uses the `<Button variant="primary">` primitive, disabled until ≥1 discipline selected. New service layer `src/services/public/services.ts` (`getPublicServicesForProperty`) does one PostgREST nested fetch — `services → service_add_ons → add_ons` — using anon-readable RLS. New placeholder seed migration `supabase/migrations/20260520120000_seed_placeholder_services_addons.sql` adds 10 services + 7 add-ons + their joins across the three properties (all prefixed `PLACEHOLDER —` so they're sweepable when Q4 lands). Disciplines page (`app/(public)/book/[property]/disciplines/page.tsx`) fetches property + services, renders the picker inside `<BookingFlowGuard requires={['bookingType']}>`, falls back to an empty-state Alert when no services are seeded. New `/when` placeholder route (`app/(public)/book/[property]/when/`) so 2.3 → 2.4 nav works; uses the new shared `<StepConfirmation>` component. **Cascade fix in `<BookingTypePicker>`:** changing booking type now clears `disciplineSelections` (different type ⇒ different selection rules); `host_an_occasion` routes directly to `/when` (skipping `/disciplines`) since exclusive-use bookings don't surface guest-driven discipline selection — easily reverted in one line if Q4 says otherwise. **Post-land review (/simplify, three parallel agents):** swapped the raw continue button for `<Button variant="primary">` primitive; dropped `aria-expanded` on service cards (`aria-pressed` is the correct toggle semantic — expansion is incidental); unwound the ternary-spread in `chooseType` into an explicit if-else for readability; extracted shared `<StepConfirmation>` component at `src/components/public/booking-flow/step-confirmation.tsx` (replaces the deleted disciplines-placeholder pattern and the dedicated `when.module.css`, reusable in 2.4/2.5). Skipped with reasoning: parallelizing the two sequential queries on `/disciplines` (real win, but the PostgREST nested-select refactor risks regressions — deferring to 2.4 when timezone + `booking_horizon_days` also need to be fetched alongside services); filtering inactive add-ons at the DB level (same reasoning, low traffic, defer); dropping the `as unknown` double-cast in the services-row coercion (reviewer was wrong — Supabase infers `add_ons` as an array for the nested join but runtime returns a single object; `as unknown` is the standard escape hatch). **Sub-phase 2.4 landed 2026-05-20:** `<WhenPicker>` at `src/components/public/booking-flow/when-picker.tsx` — two-column layout: react-day-picker v10 Calendar (new primitive at `lib/ui/primitives/calendar/`, CSS-Module-styled with brand tokens) on the left, slot grid on the right. Calendar bounds: `today` to `today + property.booking_horizon_days`. Picking a date invalidates the prior slot pick; picking a slot writes `{slotStart, durationHours}` to context (durationHours defaults from new `BOOKING_TYPE_META[type].defaultDurationHours` field — `plan_a_visit=2`, `private_lesson=1`, `host_an_occasion=2`; a real duration selector is App 2.5+ polish). Smart back link via the new shared `<StepBackLink>` component — routes to `/disciplines` for plan/lesson, straight to `/book/[property]` for host (skipping the disciplines step it bypassed forward). New `<Calendar>` primitive registered in `lib/ui` barrel. Two new migrations: `20260520130000_add_booking_horizon_to_properties.sql` adds `properties.booking_horizon_days INTEGER NOT NULL DEFAULT 30 CHECK (1..365)`; `20260520130100_seed_placeholder_time_slots_instructors.sql` cross-joins 4 placeholder slot times (9/11/13/15) across all 7 weekdays × 3 properties + adds 7 placeholder instructors (`PLACEHOLDER` prefix, sweepable when Q2 lands). New `getSlotsForProperty(propertyId)` service in `src/services/public/slots.ts` returns a `Record<dayOfWeek, AvailableSlot[]>` from one query (28 rows total). New shared `formatSlotLabel` + `formatDateLong` in `src/services/public/format.ts`. New `/details` placeholder route at `app/(public)/book/[property]/details/` gated on `bookingType + date + slotStart`. `<DetailsPlaceholder>` reads context and shows a `<StepConfirmation>` summary of the picks so far. **Plan deviation:** Calendar primitive uses a CSS Module instead of Tailwind classes (plan called for Tailwind on this primitive only) — keeps the lib/ui pattern uniform; design tokens consumed identically. **Scope reduction:** the plan's "live availability filter against bookings + instructor auto-assignment per slot" is **deferred** to a 2.4.x polish pass — `bookings` has no anon SELECT RLS policy (would need service-role or a SECURITY DEFINER RPC), and the picker is a UX hint anyway since Phase 2's triggers catch conflicts at 2.6 submission. Promote-to-RPC tagged as pre-launch hardening. **Post-land review (/simplify, three parallel agents):** extracted `<StepBackLink>` + module so `step-page.module.css` and `when-picker.module.css` no longer duplicate the back-link styling (`StepPage` uses it internally when `back` prop is set; `WhenPicker` calls it directly for its smart-back); pulled `formatSlotLabel` + `formatDateLong` into the shared `src/services/public/format.ts` (was duplicated across `slots.ts` and `details-placeholder.tsx`); renamed `getUtcDayOfWeek` → `dayOfWeekFromISO` to reflect intent rather than implementation; folded `DEFAULT_DURATION_HOURS` into `BookingTypeMeta.defaultDurationHours` so all booking-type metadata lives in one file; removed the unreachable `if (iso === state.date) return` in `handleDateSelect` (RDP v10 calls `onSelect(undefined)` on re-click — already handled by the earlier `if (!date)` guard). Skipped with reasoning: sequential property+slots queries in `/when/page.tsx` (same defer as 2.3 — batch with 2.5 data-layer revisit); `lib/ui` barrel tree-shaking concern (Next.js 16 + Turbopack handles ES re-export tree-shaking correctly); defensive null check on `state.bookingType` in `handleSlotPick` (TS narrowing — small + correct). **Sub-phase 2.5 landed 2026-05-20:** `<DetailsForm>` at `src/components/public/booking-flow/details-form.tsx` — Name / Email / Phone / Guest Count / Notes form using existing primitives (Input, Textarea, FormField, Button). Zod 4 validation (first Zod usage in the project — schema is inline `makeGuestSchema(maxGuestCount)`); errors clear field-by-field as the user edits. Per-keystroke writes to `BookingFlowProvider` context (so back-navigation preserves the form). Inline `<GuestCountStepper>` mirrors the discipline-picker pattern but takes max as a prop. Sticky `<EstimatePanel>` on the right (≥720px) — renders `Starting from $X` for flat + tiered models, `Team-quoted` for host_an_occasion, fallback `—` if no rule. Submit validates, calls `setState({guest: parsed})`, routes to /thank-you. New `src/services/public/pricing.ts` exports `PricingModel` discriminated union (flat | tiered | team_quoted), `getPublicPricingForProperty(propertyId)` (service-role read — anon has no SELECT on pricing_rules; flagged for pre-launch promotion to a SECURITY DEFINER RPC), `calculatePublicEstimate(model, inputs)` pure function, and `computeMaxGuestCount(type, model)` — both pure functions exported so the App 2.6 create-booking action can validate using the same source of truth. `formatMoney` joined the shared `format.ts` (next to `formatSlotLabel` + `formatDateLong`). New `/thank-you` placeholder route under `app/(public)/book/[property]/thank-you/` — guard requires bookingType + date + slotStart + guest; renders `<StepConfirmation>` with the full picks summary including "{time} CT" labelled. New migration `20260520140000_seed_placeholder_pricing_rules.sql` seeds public-audience rules for all three properties: private_lesson $200/hr (confirmed from Q5), plan_a_visit tiered ($150/$130/$110 per person by group size — placeholder), host_an_occasion as team-quoted (`minimum_fee=2000`, surfaced as a starting-from-only signal in the model but hidden from UI per the locked decision). **Post-land review (/simplify, three parallel agents):** moved `formatMoney` to shared `format.ts`; moved `computeMaxGuestCount` to `pricing.ts` (will be reused by the 2.6 create-booking action's validation); added a useEffect-driven clamp on `guest.count` when `maxGuestCount` changes — fixes a real back-nav bug where switching `plan_a_visit (max=12)` → `private_lesson (max=4)` would leave the stepper displaying an over-max count. Skipped with reasoning: replacing per-keystroke context writes with local state + persist-on-blur (would lose in-progress edits on accidental nav; React 19 batching handles the re-render cost at this scale); extracting a shared `<Stepper>` primitive (premature — only 2 use cases with different APIs, wait for a third); extracting `<EstimatePanel>` to its own file (tightly coupled to the form's locals); useMemo on the Zod schema (submit is rare). **Sub-phase 2.5b (funnel restructure) landed 2026-05-20:** The funnel collapses 5 steps to 4 — `/disciplines` becomes the combined "build your booking" surface, `/when` is deleted entirely. Order on the new `/disciplines`: discipline+add-on picker (hidden for `host_an_occasion`, replaced by an exclusive-use notice) → guest count stepper → calendar + time-slot grid → sticky "Estimate Total" bar with Continue. New `<BookingBuilder>` at `src/components/public/booking-flow/booking-builder.tsx` (~410 lines, single-file orchestrator with inline sections). New `<BookingSummary>` at `src/components/public/booking-flow/booking-summary.tsx` becomes the right rail on `/details` — full breakdown (type · disciplines · add-ons with **X buttons that remove an add-on from `state.disciplineSelections` live**) + "Estimate Total" headline. New shared `<QtyStepper>` primitive at `src/components/public/booking-flow/qty-stepper.tsx` (extracted during simplify; serves both add-on quantities and guest count). `pricing.ts` gains `buildBookingSummary(args)` returning the view model both the estimate bar and the right rail consume — collapses base + add-on math into one source of truth; the old `calculatePublicEstimate` is removed (callers migrated). `BookingFlowState.guestCount` promoted to a top-level field (was `state.guest.count`); `GuestInfo` slims to `name/email/phone/notes`. **Routing changes:** `<BookingTypePicker>` no longer skips `host_an_occasion` past `/disciplines` — all three types route through the combined page. `<DetailsForm>` slims to name/email/phone/notes + submit; its old inline `<EstimatePanel>` and `<GuestCountStepper>` are gone. **Deleted:** `<DisciplinePicker>` (+ CSS), `<WhenPicker>` (+ CSS), `/when` route entirely. **Disciplines page** now fetches property + services + slots + pricing in **parallel** (Promise.all) since none depend on each other — wins back the latency we deferred from 2.3/2.4. New small `BookingBuilderTypeGate` client wrapper bridges server-rendered pricing map → context-resolved booking type. Copy sweep: "Starting from" → "Estimate Total" everywhere customer-facing. **Live-availability wiring:** the calendar refetch on selection-changes is architecturally hooked but a no-op today (the `bookings` table is empty + has no anon SELECT RLS — same defer as 2.4). **Post-land review (single combined simplify pass):** extracted `<QtyStepper>` to a shared primitive — used twice in `BookingBuilder` (add-on qty + guest count) with identical surface, justified extraction. Skipped: extracting `startOfDay`/`dateToISO`/`dateFromISO` (only one consumer for now); splitting `BookingBuilder` into sub-component files (~410 lines is acceptable, sections are linear not nested); collapsing `BookingBuilderTypeGate` into `BookingBuilder` (kept the small bridge file for cleaner server/client separation). **Sub-phase 2.6 landed 2026-05-20:** The funnel writes to the database. New PL/pgSQL function `create_public_booking(...)` (migration `20260520160000_*`) atomically inserts `bookings` (Phase 2's four BEFORE triggers + instructor exclusion constraint fire here) → `booking_disciplines` → `booking_add_ons` → `bids` (Phase 3's slug trigger + bcrypt access-code hashing). PG combines `date + slot_start AT TIME ZONE 'America/Chicago'` to produce a DST-correct `timestamptz` (instead of dragging CST/CDT math into TS). Auto-assigns first active instructor for `private_lesson` when payload's instructor_id is null; RAISEs `P0002` if none exist. `booking_add_ons.unit_price_at_booking` is **derived from `add_ons.price` server-side** — payload doesn't carry prices, so a tampered submission can't rewrite the historical price snapshot. `GRANT EXECUTE` scoped to `service_role` only. New TS service `src/services/bookings/create-public-booking.ts` Zod-validates input, generates a 32-byte base64url access code (Node `crypto.randomBytes`), calls the RPC via service-role, maps Postgres error codes → typed reasons: `23P01` instructor exclusion → "instructor_unavailable"; `P0001` disambiguated by trigger message regex (`/capacity/` → "slot_taken", `/start_time/` → "invalid_start_time"); `P0002` → "no_instructor"; `23514` CHECK → "invalid_combination"; `23503` FK → same; everything else → "unknown". New Server Action wrapper at `app/(public)/book/[property]/submit/action.ts` (thin: forwards the result). `<DetailsForm>` submits via `useTransition`: validates with Zod → builds payload from context → calls the action → on `ok`, `router.push(result.redirectTo)`; on failure, surfaces `<Alert>` above the form. Submit button shows "Submitting…" + `loading` state while pending; form fields disable. New `/bids/[slug]/[code]` placeholder route validates the access code via Phase 3's `validate_bid_access_code(p_slug, p_code)` RPC (anon-granted) — returns 404 on miss, "Bid received" success state on hit (booking ID prefix + status). 2.7 fleshes this into the full bid page. Old `/thank-you` placeholder deleted — was a 2.5 holdover; the bid URL is now the funnel terminus, sitting outside `/book/[property]/` so the `BookingFlowProvider` unmounts cleanly (back-nav lands on property picker — no stale-state re-submit). **Post-land review (single combined simplify pass):** hardened `booking_add_ons.unit_price_at_booking` to derive from `add_ons` table (was trusting payload); dropped `unit_price` from `PublicBookingInputSchema` add-ons; documented the P0001 trigger-message regex assumption with the exact expected message substrings. Skipped: replacing P0001 with custom SQLSTATEs (would re-touch Phase 2 migrations — saving for a pre-launch hardening pass); server-side idempotency keys for double-submit defense (`useTransition` + `isPending` gate covers double-click; staff review backstops the rare refresh-during-submit case); inline styles on the `/bids` placeholder (explicit "this is 2.7's job" signal). **Sub-phases 2.7 + 2.8 landed 2026-05-20:** The funnel terminus is now a full bid page, not a placeholder. **2.8 first** — `src/services/bids/bid-url.ts` is the single source of truth for the bid URL shape (`/bids/<slug>/<code>`, code as path segment per the locked Referer-leak decision). Exports `buildBidUrl(slug, code)`, `buildAbsoluteBidUrl(origin, slug, code)` (forward-ready for App 8 email templates), `parseBidUrlParams(input)` (Next.js param narrowing — returns null on miss so callers 404 instead of 401), and `BID_PATH_PREFIX`. `create-public-booking.ts` no longer interpolates the path inline — calls `buildBidUrl(...)`. Grep for `'/bids/${` and `` `/bids/${ `` across the codebase returns zero results. **2.7** — `app/(public)/bids/[slug]/[code]/page.tsx` replaces the 2.6 minimal placeholder with a mobile-first, single-column bid page. Single status predicate `isActiveBid(status)` gates the body (hero + status banner always render; the body — guest summary, disciplines+add-ons nested, gear list, schedule, FAQ, map, signature slot, deposit slot — renders for `confirmed/signed/paid`; `pending_review` and `denied/expired` show hero + banner only). Sign + pay slots render in a "done" state for `signed`/`paid` rather than disappearing — the guest sees a trail of what they've completed. All times display with the literal " CT" suffix; no browser-localized abbreviations. Uses native `<details>/<summary>` for the FAQ accordion (zero JS). **New service** `src/services/bids/get-bid.ts` does the read: calls `validate_bid_access_code(slug, code)` RPC (Phase 3, SECURITY DEFINER, anon-granted, bcrypt-verify), then a single nested PostgREST select on bookings → property + booking_disciplines (services) + booking_add_ons (add_ons) + instructors via service-role (anon has no SELECT RLS on bookings — by design). Both miss paths (RPC zero rows, booking join inconsistency) return `null` and the page calls `notFound()` — never 401, never leaks slug existence. Postgres error messages are wrapped in a stable "Bid lookup failed" string before throwing (raw `error.message` would leak schema names in dev/preview error overlays). JSONB `gear_list` and `faq` columns are narrowed defensively with shape guards — staff authoring (App 3) writes objects of the shape `{name, description?}` and `{question, answer}`; anything else is dropped silently rather than crashing the bid page. Two new migrations were **not** required — Phase 3 already had everything. The bid page is OUTSIDE the funnel layout (`app/(public)/bids/` not `app/(public)/book/`), so `BookingFlowProvider` never mounts; back-nav from the bid page lands on whatever the guest came from with no stale funnel state. **Post-land review (three parallel /simplify agents — reuse / quality / efficiency):** consolidated `formatBidDateLong` + `formatBidTimeRange` + `formatBidTime` into `formatDateLongTz` + `formatSlotLabelTz` in shared `src/services/public/format.ts` (extends the funnel's `formatDateLong`/`formatSlotLabel` with timestamptz+timezone variants; deleted the now-redundant `src/services/bids/format.ts`); module-level `Intl.DateTimeFormat` cache keyed by timezone (cuts per-render instantiation cost — instances are reused across all bid page renders); replaced `BidProperty` type with re-export of `PublicProperty` (single shape for property fields across funnel + bid); collapsed identical `bodyVisible`/`embedsVisible` predicates into a single `isActiveBid(status)` with a clear comment; renamed misleading `dateShort` local (was using long format) → `dateLong`; fixed `deposit !== null` zero-check in `DepositSlot` (was `deposit ?` which treated $0 deposits as missing); grouped add-ons by `serviceId` once before render (was O(disciplines × addOns) filter inside the map); switched `get-bid.ts` to return `null` on booking-join inconsistency (was throwing — leaked Postgres detail through Next's error overlay). **Skipped with reasoning:** combining the RPC + booking fetch into one round-trip (would require a new SQL function/migration; deferred to the existing "promote service-role public reads to SECURITY DEFINER RPCs" pre-launch bucket — same class of work as the `slots.ts`/`pricing.ts` promotions already tracked); `aria-controls`/`aria-expanded` on the FAQ (native `<details>/<summary>` handles this per spec — the +/− glyph is a CSS pseudo-element, not exposed to AT); extracting `formatPrice(n, fallback)` to centralize the "team-quoted vs amount" branch (only two consumers and they read differently — premature); generated-types import for `BidStatus` (the DB enum is the source of truth, but generated Supabase types aren't wired yet — track for App 3 when admin queries need broader typing). **Manual verification 2026-05-20:** end-to-end funnel landed on the new bid page (URL built via `buildBidUrl`); 404 paths confirmed for wrong code, unknown slug, and missing code segment; all five status branches walked via Supabase SQL UPDATEs (after `ALTER TABLE bids DISABLE TRIGGER bids_sync_booking_status` to bypass the workflow guard — re-enabled after — the `paid → denied` raise is real-app-correct behavior, not a bug); populated `gear_list` + `faq` JSONB render correctly and empty states fall back gracefully; mobile breakpoint stays single-column with comfortable padding. Formal P1–P12 pack lives in 2.10. **Sub-phase 2.9 landed 2026-05-21:** Confirmation email shim. New `dev_email_outbox` table (migration `20260521080000_create_dev_email_outbox.sql`) — RLS-enabled with **no policies**, mirroring the `processed_webhooks` pattern (service-role only; drop pre-launch with the rest of `/dev`). New `src/services/notifications/send-email.ts` defines the `EmailService` interface per CLAUDE.md "Dependency Inversion" with two implementations today — `LoggingEmailService` (writes the rendered email to `dev_email_outbox` via the service-role client + renders via `@react-email/render`'s async `render()`, derives the plaintext alt from the HTML via `toPlainText()`) and `NoopEmailService` (test stub satisfying the interface unconditionally). Factory `getEmailService()` branches on `NODE_ENV === "test"`; App 8 adds a `ResendEmailService` branch — no caller change. Module is marked `import "server-only"` as a build-time guardrail against accidental client-side import (the Server-Action transitive path our booking flow uses is laundered through Next's server-only webpack layer, so the action → service → email chain is unaffected). New `src/components/email/templates/guest-booking-confirmation.tsx` — plain JSX + inline styles, table-based markup (Cormorant Garamond + Inter font stacks, brand paper background, accent CTA, bare-URL fallback for clients that strip styled buttons), props `{guestName, propertyName, dateLong, timeLabel, bidUrl}`. New `/dev/emails` review surface at `app/dev/emails/page.tsx` — gated by the existing `requireDevAuth()` cookie pattern, lists recent rows on the left, renders the selected row's `body_html` inside a `sandbox=""` iframe via `srcDoc`. New env var `NEXT_PUBLIC_SITE_URL` (documented in `.env.local`) feeds `getSiteOrigin()` in the notifications service so emails carry absolute URLs (defaults to `http://localhost:3000` in dev — production Vercel deploy sets the real host). New dep: `@react-email/render` (v2.0.8). **Renderer-research deviation from the 2.9 spec.** The plan called for `react-dom/server.renderToStaticMarkup()` with an explicit "no `@react-email/*` deps in App 2" — but React 19's `react-dom/server.react-server.js` literally `throw`s on import in any module reachable from a React Server Component graph (`"react-dom/server is not supported in React Server Components."`), and Next.js 16 / Turbopack's static-import warning fires through `"use server"` boundaries regardless of `import "server-only"`. We added `@react-email/render` (1 dep, 14 transitive) as the canonical fix: it ships server-safe entry points via package conditions AND post-processes the HTML for inbox compatibility (inline CSS, doctype, quoted-printable safety, plaintext alt). Net win: the renderer is now production-ready, the App 8 swap collapses to "transport only," and 2.10 manual testing won't surface Outlook/Yahoo/Apple-Mail-dark-mode rendering bugs that the old approach would have introduced. **Send dispatch via `after()` from `next/server`.** The email send runs post-response (after the bid-page redirect goes out) — a slow or failing send cannot delay or roll back the booking. The send helper creates its own service-role client inside the `after()` callback because closures across the `after()` boundary are not guaranteed to share request state. **Post-land review (3 parallel `/simplify` agents — reuse / quality / efficiency):** swapped `await render(element, { plainText: true })` (two React renders) for `toPlainText(bodyHtml)` (one render + html-to-text on the string — halves React CPU per send); split the `/dev/emails` list query — list view fetches metadata only, then a second `.eq(id).single()` for the selected row's `body_html` (cuts ~250–750 KB of inline-styled HTML off every page load); switched `GuestBookingConfirmation(props)` (function call) to `createElement(GuestBookingConfirmation, props)` so React handles element identity (the service file is `.ts`, not `.tsx`); collapsed `Array.isArray(data) ? (data[0] as Row | undefined) : undefined` to `(data as Row[] | null)?.[0]` (Supabase RPCs returning `TABLE(...)` always return arrays — drop the redundant runtime shape-check); dropped the `(data as { id: string }).id` cast (supabase-js types `single()` correctly); added `:focus-visible` outline on `/dev/emails` list items + dropped the mobile `max-height: 360px` touch-scroll trap on the list (use page scroll below the two-column breakpoint instead); removed dead `marginTop: "24px"` on a `<td>` in the email template (margin is ignored on table cells in most email clients); added `aria-hidden="true"` on the bare-URL fallback paragraph so screen readers don't announce the same destination twice. **Skipped with reasoning:** folding the property-name lookup into the `create_public_booking` RPC return (efficiency agent #3 — would touch the booking-creation RPC, which the 2.9 prompt explicitly forbade; one extra round-trip per booking is fine inside `after()`); CAN-SPAM compliance (unsubscribe link, mailing address) — belongs to App 8 when the real transport lands; making `dev_email_outbox.body_text` `NOT NULL` — dev-only table dropped pre-launch, App 8 builds the real notification table; shared `lib/dev/format.ts` between `/dev/page.tsx` and `/dev/emails/page.tsx` — `/dev` is dropped pre-launch, two ~10-line helpers don't earn a new module; shared `DevPageHeader` across `/dev` pages — same pre-launch-drop reasoning; `encodeURIComponent` on uuid query params (UUIDs are `[a-f0-9-]{36}` — defensive for an impossible condition); collapsing `EmailService` + classes into a single `sendEmail()` function (interface-first matches CLAUDE.md "Dependency Inversion" + the 2.9 spec, and App 8's Resend swap earns the indirection). **Manual verification 2026-05-21:** end-to-end funnel submission lands on the bid page; `dev_email_outbox` row appears with the right `to_email`, an absolute `bidUrl` in `payload`, both `body_html` and `body_text` populated; `/dev/emails` iframe renders the styled email; mobile breakpoint stacks correctly; booking still commits when the email send is forced to throw (verified by injecting a `throw` at the top of `sendConfirmationEmailBestEffort` then reverting). **Sub-phase 2.10 drafted 2026-05-21 (pending execution):** Manual test pack appended to `docs/manual-testing.md` as a new "## App 2 — Public Booking Flow" section, matching the Scenario A–F precedent. 14 scenarios: P1 (happy-path plan_a_visit with multi-discipline + add-on + 12-step verification including the email-shim payload + absolute bidUrl assertion), P2 (happy-path private_lesson with instructor auto-assignment check), P3 (happy-path host_an_occasion with capacity_reserved + zero-discipline-rows assertion + team-quoted estimate copy), P4 (capacity rejection with `you+p4a/b` two-tab pattern; relies on the seeded `max_concurrent_groups=1` so two attempts are enough), P5 (instructor exclusion race — two browser profiles, near-simultaneous submit, asserts the loser sees "instructor not available" copy not "slot filled"), P6 (discipline/add-on mismatch — documented as skippable in routine runs since the Server Action's RPC protocol makes raw curl too brittle; falls back to a "temporarily patch details-form to force a bad payload" recipe and references Phase 2 migration tests for coverage), P7 (bid URL correctness: 4 404 paths — missing code, wrong code, unknown slug, valid round-trip), P8 (cross-bid RLS: slug-A + code-B and vice versa both 404), P9 (anon REST API can't list `bids` / `bookings` / `booking_disciplines` / `booking_add_ons` / `dev_email_outbox` via the publishable key — five curl invocations), P10 (email shim payload visual review at `/dev/emails` including iframe inspection + button click + mobile breakpoint), **P10b (real-inbox preview optional polish gate — paste body_html into Gmail/Litmus, check Apple Mail dark mode, etc.; flagged as polish-gate before App 2 flips to ✅**), P11 (back-nav preserves state across 5 steps including `data-selected` on the booking-type card), P12 (refresh resets — both mid-funnel refresh and fresh-tab deep-link land on `/book/<property>?reset=1`), P-status (six-status walk with explicit `ALTER TABLE bids DISABLE TRIGGER bids_sync_booking_status` / `ENABLE` bookends — the workflow guard's `paid → denied` raise is real-app-correct behavior, manually bypassed for the test). Added an App-2-prerequisites block (placeholder seed counts to verify: ≥10 services, 84 time_slots, 7 instructors, `dev_email_outbox` exists) and an App-2-cleanup-helper SQL block (DELETE FROM bookings WHERE guest_email LIKE 'you+%@example.com' — cascades into children — plus the parallel dev_email_outbox sweep). **Verification log:** seeded a "2026-05-21 / P1–P12, P-status / ⏳ Pending" row pre-execution. **Re-run protocol** at the top of `manual-testing.md` extended: any change to `create_public_booking()`, `create-public-booking.ts`, the funnel components, the bid page, `get-bid.ts`, `bid-url.ts`, the booking-flow provider/guard, the email shim, RLS on `bookings/booking_disciplines/booking_add_ons/bids`, or Phase 2's BEFORE triggers re-runs P1–P12 + P-status. **Sub-phase 2.10 executed 2026-05-21:** All 12 runnable scenarios passed end-to-end against live Supabase + local dev server (P1 plan_a_visit, P2 private_lesson with instructor auto-assign, P3 host_an_occasion, P4 capacity rejection, P5 instructor exclusion race, P7 bid URL 4-path 404 check, P8 cross-bid RLS, P9 anon-REST 5-table empty-array smoke, P10 email shim visual review at `/dev/emails`, P11 back-nav state preservation across 5 steps, P12 refresh-resets including fresh-tab deep-link, P-status six-status walk with explicit trigger disable/enable bookends including the `tgenabled='O'` re-enable verification). **P6 skipped** by plan (the form structurally prevents mismatched discipline/add-on combos; the deferred constraint trigger has migration-test coverage and exercising it from the UI requires bypassing the form's own safeguards — not worth the patch-and-revert friction on routine runs). **P10b deferred to pre-launch** (real-inbox preview / Apple Mail dark mode / Outlook checks belong with App 8's Resend transport work; current `@react-email/render` output is good enough for App 2 sign-off). **Verification log row in `docs/manual-testing.md` flipped from ⏳ Pending to ✅ All passed.** **Finding surfaced during P4 — live availability filter UX gap:** the slot picker offers 9 AM tomorrow as selectable even when P1 already booked it; the DB constraint catches the conflict at submit (which is what P4 verified — friendly "slot just filled" copy, no 500). This is a known, deferred item — already tracked in TRACKER's "Deferred Improvements" table under `App 2 → pre-launch` (promote service-role public reads to SECURITY DEFINER RPCs, including `public_available_slots(...)`). Not a 2.10 regression; promote condition is "before public launch OR before a third service-role read caller". **Remaining:** 2.11 Vercel preview + client walkthrough. After 2.11 lands and client signs off, flip App 2 row from 🔄 to ✅. Plan in `plan/app/app-2-public-booking.md`. |
| App 3 | Admin Portal | 🔄 In Progress | Booking review, bid editor, status management. **3.8 scope changed 2026-06-01 — "preview as member" dropped** (the bid page staff inspect is already a public link; re-rendering the member portal added a parallel data path for little launch value — see CLAUDE.md Architecture Decisions). 3.8 is now a **read-only Members index + membership detail** inside `/admin`; no longer gated on App 4. Strict portal allowlist means admins never enter `/member` directly; member visibility lives inside `/admin`. See CLAUDE.md "Architecture Decisions" for rationale. Plan in `plan/app/app-3-admin.md`. **Sub-phase 3.11.x landed 2026-05-22 (admin breadcrumbs + properties layout):** Shared `<AdminBreadcrumb>` primitive at `src/components/admin/admin-breadcrumb.tsx` (+ `.module.css`) replaces the ad-hoc `<Eyebrow as="div">Admin / Foo / Bar</Eyebrow>` pattern across nine admin pages — catalog index, service edit, add-on edit, properties index, bookings index, booking detail, bids index, bid detail, bid edit. Each segment is either a `<Link>` (clickable, muted tan) or the final "current" segment (uppercase, **olive bold**, `aria-current="page"`) so "you are here" reads at a glance. Two redundant `← Back to queue` / `← Back to bid` anchor links above bid detail + bid edit removed — the breadcrumb is now the back-link. **`/admin/properties` consolidated** — was a vertical stack of three full-width cards (~70vh per card × 3 = lots of dead horizontal space at xl shell). Now a responsive grid: 1col mobile, 2col @ 880px, 3col @ 1280px with `align-items: start` so cards don't stretch. New CSS module `app/admin/properties/properties-page.module.css`. Form copy tightened for the narrower (~430px) card width: "Booking horizon (days)" → "Horizon (days)", verbose help strings shortened ("Max days into the future a public guest can book. 1–365." → "How far ahead guests can book. 1–365."; "Capacity ceiling enforced by the booking trigger." → "Capacity ceiling."), per-field "No consumer wired yet" on email/phone replaced with a single shared caption "Support contact — saved but not yet displayed to guests." below the support row. Page lead reworded ("Per-property knobs: booking horizon, capacity, tagline, support contact. Changes save per card and apply immediately."). Net: each card ~30% shorter, three properties scan side-by-side at desktop, comparison-friendly. `tsc --noEmit` clean. **Sub-phase 3.11.x landed 2026-05-22 (service hard delete + modal generalized):** Symmetry pass on the catalog hard-delete UX — services now have the same Danger zone + blocked-by-refs flow that add-ons got earlier today. New service functions `listAllBookingsForService` (no status filter) + `deleteCatalogService` (catches Postgres `23503` foreign-key-violation, returns friendly "deactivate instead" copy). New Server Actions `deleteServiceAction` + `listAllBookingsForServiceAction`. **Modal generalized** — `delete-add-on-confirm.tsx` renamed to `delete-catalog-item-confirm.tsx`; component exported as `<DeleteCatalogItemConfirm>` with a `noun` prop ("service" / "add-on"); copy interpolates the noun + plural of the other side ("All links to add-ons will be removed" for services, "to services" for add-ons) so the same modal serves both surfaces. `<ServiceEditorForm>` gains a Danger zone card mirroring the add-on form's; deactivate fallback handler preserves the current `linkedAddOnIds` so the update doesn't accidentally unlink add-ons on its way to flipping `isActive`. `<AddOnEditorForm>` updated to call the renamed component. `tsc --noEmit` clean. **Sub-phase 3.11.x landed 2026-05-22 (add-on hard delete):** Delete button added to the add-on edit page (`/admin/properties/[id]/catalog/add-ons/[addOnId]/edit`) in a "Danger zone" card below the standard Save/Cancel sticky bar. New service functions `listAllBookingsForAddOn` (no status filter — RESTRICT FK applies to historical too, so the confirm modal needs the full history) and `deleteCatalogAddOn` (catches Postgres error code `23503` foreign-key-violation and returns a user-friendly "deactivate instead" message). New `DeleteAddOnConfirm` modal — two-mode UI: **blocked path** when refs exist (shows full historical list with booking links + offers "Deactivate instead" button as the fallback action), **clean delete path** when no refs (warning copy + required confirm checkbox + red "Delete permanently" button). `service_add_ons` junction rows cascade automatically (FK is `ON DELETE CASCADE`). Two new Server Actions: `deleteAddOnAction`, `listAllBookingsForAddOnAction`. Editor form passes the deactivate fallback handler only when the add-on is still active (so a deactivate-then-try-delete flow doesn't show "Deactivate instead" pointing at an already-inactive row). `tsc --noEmit` clean. **Sub-phase 3.11.x landed 2026-05-22 (catalog UX — inline linking + follow-up plan):** First-use feedback was that creating an add-on in the right-panel library left it orphan (no `service_add_ons` row → invisible in the booking funnel) — admin's mental model conflates "create" with "make available." Fix landed today: the library "+ Add add-on" form now ends in an "Available for" section — a master "All services at this property" toggle (default checked) + a checklist of all active services. Save creates the add-on AND inserts the junction rows in one call. `createCatalogAddOn` extended with optional `linkedServiceIds: string[]` (Zod-validated array of uuids, max 200); insert happens after the row is created, atomic-enough at this scale (no transaction wrapper). UX micro-improvements that landed alongside: each library row now shows "Linked to N services" (or "Not linked to any service" in amber when N=0 + active) so unlinked add-ons stand out; help text under the library list explains that creating ≠ linking and points operators at the service edit page for retrofit links. **Bigger restructure deferred to a follow-up plan** — see `plan/app/app-3-11-1-catalog-ux.md` for: (A) service-first single-column layout with inline service cards + auto-save + a library tab as power-user secondary view, dropping the dedicated service-edit route for the common path, and (B) "Common" pill computed at render time (linked to every active service) + auto-link-common-add-ons confirm on new service creation. Both Improvements deferred until live admin usage tells us which is more painful first. `tsc --noEmit` clean. **Sub-phase 3.11 landed 2026-05-22:** Service & Add-on catalog editor at `/admin/properties/[id]/catalog`. New migration `supabase/migrations/20260522130000_catalog_property_manager_writes.sql` adds property_manager write policies on `services` / `add_ons` / `service_add_ons` (scoped via `auth_property_id()`); `service_add_ons` already had ENABLE RLS + admin write from Phase 1 — no fix needed there, plan investigation confirmed. New service `src/services/admin/catalog.ts` exposes `getPropertyCatalog(propertyId)` returning `{ services, addOns, links }`, single-row helpers `getCatalogService` + `getCatalogAddOn`, ref-count helpers `listActiveBookingsForService` + `listActiveBookingsForAddOn` (returns full booking refs with bookingId/bidId/startTime/status/guest for the deactivate modal — not just a count), and seven writers (`createCatalog{Service,AddOn}`, `updateCatalog{Service,AddOn}`, `reorderCatalog{Services,AddOns}` + the unified service-update path that handles linking + new-add-on creation atomically). Zod schemas at the module boundary; `UpdateServiceInputSchema` accepts `linkedAddOnIds: string[]` + `newAddOns: NewAddOnDraft[]` so saving a service in one click can: write name/desc/isActive, create N new add-ons in the property's library, and diff/sync the junction. Diff strategy in `updateCatalogService`: read current `service_add_ons` rows for serviceId → compute added/removed against requested → INSERT new + DELETE removed in two calls (no transaction; partial failure leaves consistent superset state, admin retries). **Server Actions** at `app/admin/properties/[id]/catalog/actions.ts` — eight thin wrappers, each `safeParse → service → revalidatePath('/admin/properties/[id]/catalog') + revalidatePath('/book/[slug]')`. Two list-refs actions (no Zod parse — read-only by id). **Page** at `app/admin/properties/[id]/catalog/page.tsx` — server orchestrator fetching property + catalog in parallel, 404 on missing property, renders two panels side-by-side at ≥1024px. **`<CatalogServicesPanel>`** + **`<CatalogAddOnsPanel>`** (client) — list with active-first rendering + Inactive divider, ↑/↓ reorder buttons, inline "+ Add" form, Edit link, Deactivate button. Per Q1 decision, Deactivate opens **`<DeactivateConfirm>`** modal which fires the list-refs action, renders each impacted booking as a row with date/time, guest name+email, status badge, and "Open →" link to `/admin/bids/[bidId]` (or `/admin/bookings/[id]` for bookings without bids); requires a checkbox second-confirm before the Deactivate button enables. **`<ServiceEditorForm>`** at `/admin/properties/[id]/catalog/services/[serviceId]/edit` — per Q2 decision, junction editing lives here, not in a separate top-level card. Form has: name + description + isActive checkbox, then a "Add-ons available for this service" section with checklist of all *active* property add-ons (linked add-ons checked), then an inline "+ Add new add-on" section that lets admin draft N new add-ons {name, description, price} which get created in the property library AND linked to this service in one save. **`<AddOnEditorForm>`** at `/admin/properties/[id]/catalog/add-ons/[addOnId]/edit` — library-level edit (name, description, price, isActive). **"Manage catalog →" link** added to the bottom of each `<PropertySettingsForm>` card pointing at `/admin/properties/[id]/catalog`. **RLS**: per Q4 decision, property_manager now has write policies (scoped to their property) on all three catalog tables. Admin/super_admin remain unrestricted via existing Phase 1 policies. **Open question parked:** Q2's "common library" framing was interpreted as per-property library (matches existing `add_ons.property_id` schema) — not a cross-property template store. If the client later wants cross-property templates (one ammunition row reused across HSB + Hog Heaven), that's a new schema concern (either `common_add_ons` table or `add_ons.template_id` self-reference). **Migration pending push**: same WSL/CLI gap as before — run `supabase db push` (or paste both `20260522120000` + `20260522130000` SQLs into the dashboard) before exercising the page; otherwise property managers will get permission errors on writes. `tsc --noEmit` clean. **Sub-phase 3.9 landed 2026-05-22:** (`/admin/properties/[id]/catalog`): admin CRUD for `services`, `add_ons`, and the `service_add_ons` junction, reached from a "Manage catalog →" link on each property settings card. Client-blocking — unblocks Q4 (full discipline catalog) by letting the client populate their own catalog. Soft-delete only (`is_active = false`) because `booking_disciplines.service_id` + `booking_add_ons.add_on_id` REFERENCES use the default RESTRICT, so historical bookings pin the row. App 2 funnel reads `is_active` already, so saved changes flow through with `revalidatePath('/book/[slug]')`. Full spec in `plan/app/app-3-admin.md` § 3.11. **Sub-phase 3.9 landed 2026-05-22:** Property settings live at `/admin/properties`. New migration `supabase/migrations/20260522120000_add_property_settings_columns.sql` adds three nullable text columns to `properties`: `tagline`, `support_email`, `support_phone` — plus seeds the three existing taglines lifted from `app/page.tsx` PROPERTY_COPY (no public-facing copy regression). New admin service `src/services/admin/properties.ts` exports `AdminProperty` (id/name/slug/timezone + 5 editable fields: bookingHorizonDays, maxConcurrentGroups, tagline, supportEmail, supportPhone), `getAdminPropertiesList(supabase)`, and `updateAdminProperty(supabase, input)` gated by a Zod schema (`UpdateAdminPropertyInputSchema`) — horizon 1-365, max groups ≥1, tagline ≤500 chars nullable, email regex nullable, phone ≤50 chars nullable. Server Action `app/admin/properties/actions.ts` is the standard `safeParse → service → revalidatePath('/admin/properties') + revalidatePath('/')` wrapper. Form component `src/components/admin/property-settings-form.tsx` is a client component reusing the existing `bid-editor-form.module.css` styles; uses `useTransition` for the save, renders an inline success `<Alert>` on save and an error one on failure. Page at `app/admin/properties/page.tsx` (server) fetches via the service and renders one `<PropertySettingsForm>` per property in a vertical stack with the `<PropertyPill>` as each card's heading. **Public path: `PublicProperty` gained a `tagline: string | null` field** + `getPublicProperties` selects + maps it; `app/page.tsx` drops `tagline` from `PROPERTY_COPY` (locale + href stay) and now reads `p.tagline` directly from the row (renders nothing when null, so admin can blank a tagline to hide that line on the home page card). Get-bid (`src/services/bids/get-bid.ts`) also extended to select + map tagline since `BidProperty = PublicProperty` — kept the type alias intact rather than forking it. **RLS**: Phase 1's `properties: admin write` policy (super_admin + admin) covers the UPDATE; property managers see the page but the UPDATE rejects with a Postgres permission error — surface for property managers gets the editable inputs today (no role-gated rendering); promote to read-only-for-managers if needed. **Nav unlocked** — Properties item flipped from `disabled: true` to live. **Migration pending push**: Supabase CLI has no access token in this WSL env, so `supabase migration list` and `db push` both prompt for login. Run `supabase db push` (or paste the SQL into the dashboard SQL editor) before hitting `/admin/properties` — the service's SELECT will fail with `column "tagline" does not exist` otherwise. `tsc --noEmit` clean. **Sub-phase 3.7 landed 2026-05-22:** Bookings index lives at `/admin/bookings`. New service `src/services/admin/bookings.ts` exports `getAdminBookingsList(supabase, filters)` — same nested-query shape as 3.2 but reversed: starts from `bookings` (not `bids`), left-joins `bids ( id )` to surface the optional attached bid id, joins `properties!inner`. Filters: `status` (8 booking_status_enum values — `pending_review`, `awaiting_guest`, `signed`, `deposit_paid`, `fulfilled`, `denied`, `cancelled`, `expired`), `propertyId`, `type` (new — `plan_a_visit` / `private_lesson` / `host_an_occasion`), `from`/`to` (booking start_time bounds), `q` (guest_name OR guest_email ilike, same `% ( ) ,` sanitization as 3.2.x). Default sort: `start_time ASC` (upcoming first) — different from bids which sorts `created_at DESC`. Same pagination envelope as `getAdminBidsList`. New `<BookingStatusBadge>` at `src/components/admin/booking-status-badge.tsx` because the 8 booking statuses don't overlap cleanly with the 6 bid statuses — pending_review→filling, awaiting_guest→waitlist, signed→tierCharter, deposit_paid→open, fulfilled→past, denied+cancelled→full, expired→past. New `<BookingFilters>` + `<BookingListTable>` at `src/components/admin/booking-{filters,list-table}.tsx`. Filter form adds a Type dropdown next to Property; status pills, search, date range, Reset/Apply identical to bid queue. Table columns reordered per plan: **When first** (date + slot stacked), **Status second** (badge), then Booking type, Property, Guest, Created. Action column routes smartly: if `row.bidId` is non-null → `View bid →` linking to `/admin/bids/[bidId]`; otherwise → `View →` linking to `/admin/bookings/[id]`. Detail page at `app/admin/bookings/[id]/page.tsx` follows the plan's "minimal until a real use case emerges" — if a bid is attached, the page calls `redirect('/admin/bids/[bidId]')` (the bid detail is the canonical surface); otherwise renders a read-only Booking / Guest / Pricing / Timeline card stack via `getAdminBookingDetail` in `src/services/admin/get-booking-detail.ts`. **Bookings nav unlocked** — `<AdminNav>` Bookings item flipped from `disabled: true` to a live link. **CSS module rename** — `bid-list.module.css` → `queue-list.module.css` since the classes are queue-list primitives (filters, table, pagination) reused by both bid + booking lists; updated 3 importers (`bid-filters.tsx`, `bid-list-table.tsx`, `app/admin/bids/page.tsx`). **`AdminBookingType` re-export** — `bookings.ts` re-exports the type from `bids.ts` so the bookings page can import everything from one module. RLS: the auth-aware server client lets Phase 2's `bookings: admin read all` / `bookings: property_manager read` policies do the scoping — property managers see only their property's bookings; concierges + members see their own per Phase 2 policies. `tsc --noEmit` clean. **Dev smoke-test gated:** WSL environment has Node 18.19; Next 16 dev server requires Node ≥20.9 — couldn't boot `npm run dev` to walk the route in a browser here. Type-check clean and structure mirrors 3.2 line-for-line; user should hit `/admin/bookings` in their primary dev environment to confirm. **Sub-phase 3.2.x landed 2026-05-21 (search):** Added `q` filter to the bid review queue — guest_name OR guest_email `ilike '%q%'`. Service-side `.or('guest_name.ilike.%X%,guest_email.ilike.%X%', { referencedTable: 'bookings' })` runs on the joined bookings table; the query value is sanitized server-side (strips `% ( ) ,` so a search string can't escape the PostgREST OR expression). New "Search" input next to Property in `<BidFilters>`; `q` participates in status pill URLs + pagination URLs so the search persists across filter changes. **Loading indicator (same sub-phase):** `<BidFilters>` converted to a client component using `useTransition` + `useRouter.push` for form submit. Apply button uses the Button primitive's existing `loading={isPending}` prop + label flips to "Searching…". A small dashed-border "Loading…" chip appears next to the status pills row whenever a transition is in flight. Status pill clicks still use native `<Link>` navigation (no in-page indicator); could promote with `useLinkStatus` later but the current surface is good enough. **Sub-phase 3.6.4 landed 2026-05-21 (today time-grid + vivid status colors):** **(1) Confirmed section restructured into a 3-column time-grid by property** — `<DaySchedule>` at `src/components/admin/day-schedule.tsx` + module CSS. Each property column renders a `position: relative` time canvas with absolutely-positioned hour labels + dashed hour lines + colored booking blocks. Hour band auto-fits: starts with a 7 AM – 8 PM default and expands if a booking falls outside. Block top + height computed from `(startHour - dayStartHour) * HOUR_HEIGHT` and `durationHours * HOUR_HEIGHT` so a 3-hour booking literally spans 3 grid rows. Booking blocks get property-tinted backgrounds + 4px left accent (matches PropertyPill palette). A **red "now" line** (2px solid + 8px dot) draws across the grid when the column's date matches today's date in CT. Inline CSS custom properties (`--top`, `--block-height`, `--grid-height`) keep the dynamic positions out of `style` strings except for the numeric values themselves. Mobile breakpoint inherits `.columnGrid` from the upcoming section — 3 columns ≥ 720px, single column below. **Service changes** — `getAdminDashboardData` now returns `confirmedTodayByProperty: PropertyColumn[]` + `confirmedTomorrowByProperty: PropertyColumn[]` (the old flat `confirmedNext24h: AdminBidListRow[]` field dropped from the type — page no longer reads it). Bucketing uses a single `DASHBOARD_TIMEZONE = 'America/Chicago'` constant since all 3 properties are CT today; promote to per-row property timezone if a property ever adopts a different zone. Tomorrow section only renders when at least one tomorrow column has bookings. **(2) Status colors made vivid + distinct + 2x thicker per feedback** — left stripe doubled from 4px → 8px. Color palette swapped from the muted brand-friendly tones to a high-saturation set: pending → bright yellow `#eab308`, confirmed → bright green `#16a34a`, signed → bright blue `#2563eb`, paid → purple `#7c3aed` (terminal celebration), denied + expired → bright red `#dc2626`. Legend stripes mirror these. The five colors are easy to differentiate at peripheral glance. `tsc --noEmit` clean. **Sub-phase 3.6.3 landed 2026-05-21 (activity stripe + legend):** Recent Activity status indicator demoted from a full-width pill to a **4px left-border stripe** on each row. The stripe color follows the status color system (pending→amber, confirmed/paid→green, signed→blue, denied/expired→red). The row grid drops the status column entirely — now `[guest+type 1fr][property auto][time auto]` — so the property pill reads first as the categorical anchor and the guest name has room to breathe without truncation. Status is still announced to screen readers via `aria-label={`${statusLabel} bid for ${guestName}`}` on the row link. A small **legend** strip below the activity list spells out the color code: "STATUS" eyebrow + 6 mini-stripe+label pairs (one per status). Mobile breakpoint @ 720px stacks main on row 1 + property/time on row 2 — status stripe stays as the unbroken left edge. **Sub-phase 3.6.2 landed 2026-05-21 (polish):** Wider admin shell + color-coded property pills + Recent Activity row redesigned for readability + bid status badge color adjustments. **(1) Wider admin shell** — new `--content-xl: 1440px` token in `app/globals.css` + new `"xl"` PageShell width variant in the `PageShellWidth` union (`lib/ui/primitives/page-shell/page-shell.tsx` + `.module.css`). Admin pages (dashboard, queue, bid detail, edit) opt in via `<PageShell width="xl">` for ~22% more horizontal breathing room on wide displays without affecting public/member surfaces. **(2) `<PropertyPill>` component** — `src/components/admin/property-pill.tsx` + module CSS. Maps property slug → color variant: `horseshoe-bay` → olive-tinted (brand green family), `hog-heaven` → tan/wheat-tinted (warm gold), `packsaddle` → slate-blue (cool precision). Unknown slugs fall through to a neutral `--paper-warm` tint. Optional `withDot` prop renders a small solid color disc inline (used on the upcoming-by-property column headers where the pill is the column's identity). Optional `bare` variant strips fill/border for in-heading contexts. Pills surface on: dashboard pending mini-row meta, timeline meta, property-column headers, activity rows, bid list table Property column, bid detail Booking card. **(3) Recent Activity row redesigned** — was a cramped `120px 1fr auto` grid with guest/type/property concatenated into one inline string with `·` separators, which forced long property names to truncate ("Packsad...", "Hog Heaven..."). Replaced with a proper 4-column grid `[status 140px] [guest+type stacked, 1fr] [property pill, min-content] [time, auto]` + `gap: var(--space-4)`. Status badge fixed-width so the column scans cleanly top-to-bottom. Guest name + booking type stack in their own min-width zone with overflow-ellipsis on each line. Property pill gets its own column so it never truncates and the color reads first. Time stays right-aligned. Mobile breakpoint @ 720px reflows to a 2-row layout (status + main on row 1, property + time on row 2). **(4) Status badge colors** — confirmed flipped from `tierCharter` (blue) → `open` (green) and expired from `past` (gray) → `full` (red) per direct ask. Paid stays `open` (green) — confirmed/paid both signal positive states. Signed stays `tierCharter` (blue) as the distinct "progress made, not yet paid" middle state. Denied + expired both `full` (red) — semantically both terminal "not happening" states; can introduce a distinct gray-tinted "expired" variant later if disambiguation matters. `tsc --noEmit` clean. **Sub-phase 3.6.1 landed 2026-05-21:** Polished the dashboard per feedback — confirmed-24h promoted to a **timeline view** (big serif hour on the left with a vertical olive divider rule + body on the right showing guest, type, property, party size, duration; "Today"/"Tomorrow" or short date labels via `timelineDateLabel(iso, tz)` which compares the property-tz date against today's tz-formatted date), and upcoming-7d promoted to a **3-column property grid** (always renders all 3 properties even when empty so staff see "Packsaddle is quiet this week" at a glance; columns stack to 1col below 720px). Layout restructured: top row is a 2-card split (Pending review + Recent activity), then full-width Timeline card, then full-width Property grid. Per-card row counts increased — confirmedNext24h cap from 5 → 15, upcomingWeek from 5 → 30 (split across 3 properties = ~10 each). New `<PropertyColumn>` data shape on `AdminDashboardData` (`{ propertyId, propertyName, propertySlug, rows }[]`) computed server-side from the `upcoming` query + a parallel `properties` fetch so the three columns persist regardless of distribution. **Zebra striping** added across admin lists per same feedback pass — `:nth-child(odd|even)` row backgrounds using `--paper-warm` for the off-row + `--cream` for hover, applied to: bid review queue `<table>` rows (`bid-list.module.css`), dashboard mini-list (Pending), timeline, property column rows, activity list. Hover state on every list row is now `--cream` (slightly warmer than the off-row so it still reads as interactive). `tsc --noEmit` clean. **Sub-phase 3.6 landed 2026-05-21:** Replaced the single-card placeholder at `/admin` with a real-data dashboard. New service `src/services/admin/dashboard-data.ts` exposes `getAdminDashboardData(supabase)` returning four parallel queries in one trip: **Pending review** (status=pending_review, sort `created_at DESC`, top 5 + count), **Confirmed next 24h** (status=confirmed, `bookings.start_time` between now and now+24h, sort start_time ASC, top 5 + count — uses `referencedTable: 'bookings'` syntax on `.order()` to sort by the nested foreign-table column), **Upcoming next 7 days** (status IN confirmed/signed/paid, start_time between now and now+7d, sort start_time ASC, top 5 + count), **Recent activity** (any status, sort `updated_at DESC`, top 10 — the "rough approximation" path called out in the plan; a proper `bids_status_history` audit table sits in the Deferred Improvements row, gets promoted when a second staff member starts touching bids regularly or a dispute reconstruction needs reliable history). All four queries run via auth-aware client so Phase 3's RLS scopes per role (admin/property_manager/concierge/etc.) — property managers see their property's slice; membership coordinators see an empty dashboard. **Timezone simplification:** uses simple `now → now+24h` and `now → now+7d` windows instead of timezone-aware day boundaries. The dashboard is a glance-and-go tool, not a billing report — precision-day boundaries weren't worth the SQL function or `Intl.DateTimeFormat` boundary math today. Promote to property-tz day buckets when the dashboard accumulates a second time-windowed widget where "today" specifically matters. New `src/components/admin/dashboard.module.css` styles a responsive grid (1col → 2col @ 720px → 3col @ 1080px) with the Recent activity card spanning full width via `grid-column: 1 / -1`. Local `<MiniRow>` + `<MiniList>` + `<ActivityRow>` components inline in `app/admin/page.tsx` since the dashboard is the only consumer (extract if a second page wants the pattern). Each card: eyebrow + count headline + 5-row mini-list + "View all →" link to a pre-filtered queue URL (e.g. `/admin/bids?status=pending_review`). Activity rows render the `<BidStatusBadge>` + guest name + booking type + property + `formatRelative(updatedAt)` ("3m ago", "2d ago"). `formatShortWhen(iso, tz)` strips the weekday + year from `formatDateLongTz` so list rows stay compact ("May 22 · 9 AM CT" vs the detail page's full "Friday, May 22, 2026 · 9 AM CT"). Quick-action buttons from the plan ("+ New booking", "+ New member") deliberately skipped — they reference 3.7/3.8 surfaces that don't exist yet; adding disabled placeholders would be more visual noise than signal. The `getAdminDashboardCounts` function in `src/services/admin/dashboard.ts` stays put — it's still the auth layout's pending-count source (every admin page navigation reads it for the AdminNav badge). The dashboard page does a redundant pending-count query inside `getAdminDashboardData`; both happen in parallel within a single layout+page render so the wire cost is negligible. `tsc --noEmit` clean. **Sub-phase 3.5 landed 2026-05-21:** Bid status transitions wired up. Three actions live on the bid detail page via the new `<BidActions>` client component: **Confirm** (pending_review only), **Deny** (pending_review only, requires a reason), **Regenerate URL** (any active status — pending_review/confirmed/signed; hidden for paid/denied/expired since the URL is moot there). New service `src/services/admin/transition-bid.ts` exposes three pure functions — `confirmBid` and `denyBid` go straight through the auth-aware client's UPDATE on bids (Phase 3's `bids: staff update` + Phase 3's `bids_sync_booking_status` trigger keep booking.status in lockstep automatically — no manual booking write needed). `denyBid` also stamps `denial_reason` + `cancelled_at`. **Regenerate URL needs pgcrypto** for the bcrypt hash, so it goes through a new SECURITY DEFINER RPC `regenerate_bid_access_code(p_bid_id, p_code)` introduced in migration `supabase/migrations/20260521130000_regenerate_bid_access_code_rpc.sql`. The function inlines Phase 3's staff-scope check (super_admin/admin pass globally; property_manager re-validated against `b.property_id = jwt.property_id`) since SECURITY DEFINER bypasses RLS. Returns the bid id on success; raises `42501` on unauthorized + `P0002` on not-found. The plaintext access code is generated server-side in `transition-bid.ts` via `crypto.randomBytes(32)` → base64url (same generator as `create-public-booking.ts`'s `generateAccessCode`) and supplied to the RPC, which hashes it via `extensions.crypt(p_code, extensions.gen_salt('bf', 10))`. Slug stays put — only the access code rotates — so the bid's stable identity doesn't move. New Server Action wrapper `app/admin/bids/[id]/actions.ts` exports `confirmBidAction`, `denyBidAction`, `regenerateBidUrlAction`. `regenerateBidUrlAction` reads `process.env.NEXT_PUBLIC_SITE_URL` (with a `x-forwarded-proto` + `host` header fallback) to build an **absolute** URL via `buildAbsoluteBidUrl` so the URL admins copy out works for guest delivery. All three actions call `revalidatePath('/admin/bids/[id]')` + `revalidatePath('/admin/bids')` + `revalidatePath('/admin')` on success — list view, dashboard count, and detail page all refresh. **`<BidActions>` component state machine:** `'none' | 'confirm' | 'deny' | 'regenerate' | 'regenerate-success'`. Each transition opens a dialog (or success panel for regenerate). Confirm + Deny dialogs ask one question with a clear "What this does" paragraph; Deny adds a required textarea (Save disabled until non-empty). Regenerate dialog warns the URL will rotate; success panel shows the new URL in a mono-font box with a Copy button (uses `navigator.clipboard.writeText`, soft-fails in non-secure contexts) and an italic warning **"This URL is shown once. Copy it now — we can't recover the access code after you leave this page."** New CSS module `src/components/admin/bid-actions.module.css`. Detail page (3.3) action area: edit button stays as-is, the three disabled `(3.5)` placeholders replaced with `<BidActions bidId={bid.id} status={bid.status} />`. Bid lifecycle is now fully wired end-to-end: App 2 funnel creates `pending_review` bid → App 3.1-3.4 staff reviews + edits → App 3.5 transitions to `confirmed` / `denied` (or rotates the URL on a confirmed/signed bid) → Phase 3's `bids_sync_booking_status` trigger keeps `bookings.status` in lockstep → public bid page (App 2.7) reflects the new status on next guest visit. **Email side-effects deferred to App 8.** Confirm + deny notifications could route through the `LoggingEmailService` shim from 2.9, but: (a) Confirm needs the bid URL, which we **can't reconstruct** post-creation because the access code is bcrypt-hashed at rest (only the create-time plaintext or a fresh regenerate produces a URL); (b) Deny is a more careful copy decision than 3.5 should make. The guest already has the URL from their App 2.9 booking confirmation; reloading the bid page shows the new status. Real notification flow gets a proper sub-phase under App 8. **Regenerate-URL email**: also deferred — the admin sees the new URL on screen and copy/pastes it to the guest manually until App 8 wires Resend. **Migrations to push**: `20260521120000_add_quote_note_to_bids.sql` (3.4.x) and `20260521130000_regenerate_bid_access_code_rpc.sql` (3.5) — run `supabase db push` before exercising the editor's quote-note field or the Regenerate URL action. `tsc --noEmit` clean. **Sub-phase 3.4.x landed 2026-05-21 (markdown + quote-note + estimate strikethrough):** Three small but useful additions to the bid editor / detail / public-bid surfaces. **(1) Estimate strikethrough on the admin bid detail page** — when `bookings.confirmed_price !== null AND !== estimated_price`, the Estimated row in the Pricing card renders with `text-decoration: line-through` + `opacity: 0.55`. If admin saved the same number (no real adjustment) or left confirmed blank, no strikethrough (would be redundant). One new `.priceStruck` class in `bid-detail.module.css`. **(2) Markdown rendering for staff-authored text fields** — schedule notes, gear list descriptions, FAQ answers, staff notes, denial reason, and the new quote note all support markdown (links, bold, italic, lists, code, GFM tables). New deps `react-markdown@10` + `remark-gfm@4` (~40kb gzip added to admin + bid-page bundles). New shared component `src/components/shared/markdown.tsx` exports `<MarkdownProse>` with `small?: boolean` variant for in-list contexts (gear/faq item bodies). Hardening: `urlTransform` filters URIs to `^(https?:|mailto:|tel:)` so a `javascript:` href can't slip in; `target="_blank" rel="noopener noreferrer"` on every `<a>` so opens never trade a window-opener handle. New `markdown.module.css` styles the prose surface to the brand (Inter + accent-info links + warm code blocks + brand list spacing). Applied to: admin detail page (schedule notes, gear desc, faq answer, staff notes, denial reason), public bid page (schedule notes, gear desc, faq answer). Editor textareas updated with help text mentioning markdown + `**bold**` / `[text](https://…)` / lists examples. Gear description input upgraded from a single-line `<input>` to a 2-row `<textarea>` since multi-line markdown wouldn't fit otherwise. **(3) Optional quote note (`bids.quote_note text`)** for explaining a price adjustment ("Holiday weekend rate" / "Group of 10+ surcharge" / "Returning-guest discount"). New migration `supabase/migrations/20260521120000_add_quote_note_to_bids.sql`. Editor: new `<textarea>` inside the Pricing card with placeholder examples + help text ("Shown to the guest next to the confirmed quote — use this to explain a price adjustment from the estimate. Markdown supported."). 500-char cap (Zod `.max(500)`). Service `updateAdminBid` writes `quote_note: input.quoteNote ?? null`. Both `getAdminBidDetail` (admin) and `getBidDetail` (public) services + interfaces gained `quoteNote: string | null`; public path coerces `?? null` against the case where the migration hasn't been applied yet (graceful degrade). Phase 3's `validate_bid_access_code` RPC returns `SETOF bids` so the new column flows through automatically — no RPC migration required. Admin Pricing card renders the quote note via `<MarkdownProse small>` directly below the confirmed amount; public bid page renders it inside `<DepositSlot>` above the meta line (where it gives context to the deposit ask). `tsc --noEmit` clean. **Migration is staged but not yet pushed** (`supabase db push` required before the editor's quote-note save will land cleanly — Postgres returns a column-doesn't-exist error otherwise; the get-bid services already handle missing-column gracefully via `?? null`). **Sub-phase 3.4 landed 2026-05-21:** Bid editor lives at `/admin/bids/[id]/edit`. New service `src/services/admin/update-bid.ts` exports a Zod schema (`UpdateAdminBidInputSchema`) + the writer function `updateAdminBid(supabase, input)`. **Schema reality check vs. the plan doc:** the plan called for a `bids.quote_amount` + `bids.deposit_amount` edit, but the live schema stores those on `bookings.confirmed_price` + `bookings.deposit_amount` — `bids` carries only `schedule_notes`/`gear_list`/`faq`/`staff_notes`/`denial_reason`. The editor accommodates: writes touch **two tables** (`bookings` for prices, `bids` for content) via two sequential UPDATEs against the auth-aware client (Phase 2's `bookings: staff update` + Phase 3's `bids: staff update` RLS gate both writes; admin/super_admin pass globally, property_manager passes only for their property). No transaction wrapping — if the second UPDATE fails the page renders an Alert and the operator retries; loss of atomicity is acceptable for a low-frequency operator action, and tightening would move both writes into a SECURITY DEFINER function. **Zod money field** is a `z.union([string, null, undefined])` that transforms to `number | null` — accepts the raw form-input string from the client without a manual cast at the call site. Two type aliases exported: `UpdateAdminBidInput` (output, used by the service) + `UpdateAdminBidRawInput` (input, used by the action signature so the client doesn't have to pre-parse). Gear list cap 20 items, each name 1-200 chars + optional description 0-500; FAQ cap 20 items, question 1-500 + answer 1-2000; notes 0-5000. Items with empty `name` / empty `question`+`answer` are silently dropped client-side before the server call. Server Action wrapper `app/admin/bids/[id]/edit/actions.ts` (thin: `safeParse` + delegate to service + `revalidatePath` on success for both `/admin/bids/[id]` and `/admin/bids`). **Client component** `src/components/admin/bid-editor-form.tsx` is the form — controlled inputs across pricing / schedule notes / gear list (repeater of {name, description}) / FAQ (repeater of {question, answer}) / staff notes. Repeater UX: each item is a card with inputs + a "Remove" button; "+ Add" button appears below until the cap is hit. Uses `useTransition` for the save flow — Save button gets the Button primitive's `loading={isPending}` + label flips to "Saving…", Cancel button gets `disabled={isPending}`. On success: `router.push('/admin/bids/[id]')` + `router.refresh()` so the detail page picks up the fresh data. On failure: Alert renders inline above the form with the server-returned error message. **Sticky action bar at the bottom** (`position: sticky; bottom: var(--space-3)`) so Save/Cancel stay reachable on long FAQ lists. New CSS module `src/components/admin/bid-editor-form.module.css`. New `app/admin/bids/[id]/edit/page.tsx` is the server orchestrator (fetch detail via `getAdminBidDetail`, 404 on null, render the form). Edit button on the bid detail page (3.3) was flipped from a `disabled` placeholder to a working `Button asChild + Link` pointing at `/admin/bids/[id]/edit`. `tsc --noEmit` clean. **Sub-phase 3.3 landed 2026-05-21:** Bid detail page at `/admin/bids/[id]`. New service `src/services/admin/get-bid-detail.ts` exports `getAdminBidDetail(supabase, bidId)` returning an `AdminBidDetail | null` shape — bid (id, slug, status, schedule_notes, gear_list, faq, staff_notes, denial_reason, refund_amount, expires_at, signed_at, cancelled_at, created_at, updated_at) + booking (id, type, start/end_time, duration, guest fields, audience_type, capacity_reserved, three price columns) + property + disciplines + add_ons (grouped by service_id at render time) + instructor. Mirrors the public `getBidDetail` shape from 2.7 but lookup is by bid `id` (not slug+code), uses the auth-aware Supabase client so RLS scopes per role (admin/property_manager/concierge filter at the DB), and surfaces admin-only fields (staff_notes, denial_reason, refund_amount, audience_type, capacity_reserved) that the public bid page hides. Same defensive `parseGearList` + `parseFaq` JSONB narrowing as `get-bid.ts` (strings dropped silently, not crashed). New CSS module `src/components/admin/bid-detail.module.css` with a two-column grid layout at ≥900px (`grid-template-columns: 2fr 1fr`) — main column for Booking / Disciplines + Add-ons / Bid content, side column for Guest / Pricing / Lifecycle. `.span2` class lifts wide cards (Disciplines + Add-ons, Bid content, Staff notes) to full width. Page at `app/admin/bids/[id]/page.tsx` is a server component: orchestrates the fetch, calls `notFound()` on null (404 covers RLS-deny too — property manager hitting a foreign bid id gets the same response as a non-existent id), renders the section grid. Header carries the guest name as the h1, status badge + slug pill inline, and three **placeholder action buttons** disabled for 3.4/3.5 wiring (`Edit (3.4)`, `Confirm (3.5)` + `Deny (3.5)` — last two only render for `pending_review`). Add-ons are pre-grouped into a `Map<serviceId, AdminBidAddOn[]>` before render so the nested list is O(n) not O(d × a). Lifecycle card shows created/updated/signed/cancelled/expires timestamps formatted via the existing `formatDateLongTz` + `formatSlotLabelTz`. Email + phone in the Guest card are wrapped in `mailto:` / `tel:` anchors. Staff notes section carries a 🔒 banner "Internal — not shown to the guest." Denial reason renders only when `status='denied'` AND `denial_reason` is non-null. **Back-to-queue link** points to bare `/admin/bids` (loses prior search context — known paper cut; promote to a `?return=` carry-forward later if staff complain). **Public preview link deliberately omitted from 3.3.** The bid's plaintext access code is bcrypt-hashed at rest (Phase 3); admins can't reconstruct the `/bids/<slug>/<code>` URL from the DB alone. Surface arrives with 3.5's "Regenerate URL" action which returns a fresh plaintext code one-time. `tsc --noEmit` clean. **Sub-phase 3.2 landed 2026-05-21:** Bid review queue lives at `/admin/bids`. New service `src/services/admin/bids.ts` exports `getAdminBidsList(supabase, filters)` returning typed rows via one nested PostgREST query — `bids` joined to `bookings!inner` joined to `properties!inner`, ordered by `created_at DESC`, with `count: 'exact'` and a `range(from, to)` window for pagination. Filters: `status`, `propertyId`, `from`/`to` (booking start_time bounds in UTC, converted via `${date}T00:00:00Z`/`T23:59:59Z`). The nested `.eq('bookings.property_id', ...)` filter combined with `!inner` reduces parent rows, so the count + page are accurate for property-scoped filters. Returns `{ rows, totalCount, page, pageSize, hasMore }`. **Result envelope on failure:** the page-level catch flattens the thrown error into `{ error: string, rows: [], totalCount: 0, ... }` so the page can render the error in an Alert without a partial table — `"error" in list` narrows it. New components in `src/components/admin/`: `<BidFilters>` (server component — status pills as `<Link>` elements, property dropdown + date range inside a `<form method="GET">` that re-submits to the same path; hidden `status` field preserves the pill selection across filter applies), `<BidListTable>` (server, columns Guest / Booking / When / Property / Status / Created / →; empty state inside the table frame for consistency), `<BidStatusBadge>` (maps `AdminBidStatus → BadgeVariant` — pending_review→filling, confirmed+signed→tierCharter, paid→open, denied→full, expired→past; chosen from existing Badge variants instead of inventing new ones). Tiny local `formatRelative(iso)` for "3h ago" / "2d ago" — local because it's one consumer. Format reuses existing `formatDateLongTz` + `formatSlotLabelTz` from `src/services/public/format.ts` (no new format helpers). New CSS module `src/components/admin/bid-list.module.css` styles filter chrome + table + pagination — pill row, advanced filter form, hover-highlighted table rows, sticky status colors, mobile-friendly horizontal table scroll. Page at `app/admin/bids/page.tsx` is a server component: `parseFilters(searchParams)` (narrowed via `isBidStatus` type-guard against the `ADMIN_BID_STATUSES` const), parallel `Promise.all` fetches bids + property dropdown options, renders `<BidFilters>` + summary line ("Showing 1–25 of 47") + `<BidListTable>` + Previous/Next pagination. **Pagination URL strategy:** `buildPageHref(filters, nextPage)` preserves filters and sets `?page=N` (omits page=0 for clean default URLs); `buildStatusHref(basePath, current, nextStatus)` from `<BidFilters>` preserves property+date but **clears page** (changing status resets to page 0 — applying new filters should not retain a stale offset). **RLS interaction (verified by design — manual P-pack lands in 3.10):** the page uses the auth-aware `createServerSupabaseClient`, so Phase 3's `bids: admin read` policy gives super_admin/admin global scope, `bids: property_manager read` restricts property managers to their own property's bids (the propertyId dropdown is still rendered for them but RLS empties non-matching selections), `bids: concierge read own` returns only bids the concierge created, `bids: member read own` would return member-created bids (no surface today), `bids: partner read own` similar for partner-channel bookings, and membership_coordinator has no policy at all so they see the friendly empty state. The propertyId / from / to filter terms apply on top of RLS, never below — RLS is the floor. `tsc --noEmit` clean. **Sub-phase 3.1 landed 2026-05-21:** Replaced the JWT-claim stub at `/admin` with a real portal shell. New `app/admin/layout.tsx` is a server component that fetches user + role + pending-bid count in parallel (Promise.all) and mounts `<AdminNav>` from `src/components/admin/admin-nav.tsx` above `{children}`. AdminNav is a client component (needs `usePathname()` for active-state) with a sticky top bar: Rhythm wordmark + "ADMIN" lockup on the left, nav items (Dashboard, Bids, Bookings, Members, Properties) in the middle, identity strip (email + role) + sign-out on the right. Bookings/Members/Properties render as `aria-disabled` "soon" pills today — placeholders for 3.7-3.9. The Bids item carries a count badge (`accent-warn` background) when pending_review > 0; in the active state the badge inverts to `paper` background. New service `src/services/admin/dashboard.ts` exports `getAdminDashboardCounts(supabase)` — uses the auth-aware server client so Phase 3's `bids: admin read` / `bids: property_manager read` RLS policies do the scoping automatically (admins see global count, property managers see their property's count, membership coordinators see 0 because they have no `bids` read policy — visible-empty-queue is the correct surface). New `app/admin/page.tsx` replaces the stub: a grid of dashboard cards starting with "Bids awaiting review" (count + "Open queue →" button using `Button asChild` so the semantic markup stays anchor-inside-no-button). New `app/admin/bids/page.tsx` is a "lands with 3.2" placeholder so the nav link doesn't 404 during 3.1 verification. Top-bar collapses on `<= 720px`: links wrap below to a horizontally-scrolling row, identity strip drops `margin-left:auto`. RLS verification path: `getAdminDashboardCounts` calls `bids.select('id', { count:'exact', head:true })` — RLS filters before count happens, so `pendingBids` is automatically what the signed-in role is allowed to see. `tsc --noEmit` clean. **Sub-phase 3.12 landed 2026-06-01 (Bookings Calendar):** `/admin/bookings` reworked from a list into a color-coded month calendar with an hourly day drill-down, replacing the list/table (resolves the "bids vs bookings feels duplicate" confusion — Bids stays the work queue, Bookings is now the ops calendar). **Data layer** `src/services/admin/bookings-calendar.ts`: `getAdminMonthBookings(supabase, {propertyId?, year, month})` wraps `getAdminBookingsList` with month bounds (padded ±1 day so timezone bucketing catches month-edge bookings under the service's UTC `from`/`to` filter) + `pageSize: 1000`; `computeDayDensity(rows, properties)` (pure) → `Map<"YYYY-MM-DD", DayCell>` where `DayCell = { total, byProperty, density }` — buckets **active** bookings only (status NOT IN cancelled/denied/expired) by each row's **property timezone** (`Intl en-CA`), seeds `byProperty` to 0 for known properties; `bookingCalendarDate(row)` exposes the same tz-bucket for the day-detail filter. Count→density via a single `DENSITY_THRESHOLDS` constant (0=empty/paper, 1–2=light/tan, 3–4=busy/amber, 5+=full/olive-deep) — the whole metric lives behind `computeDayDensity()` so it swaps to true slot-utilization once client Q2 (operating hours / `time_slots`) lands, with no UI change. **Month UI** `src/components/admin/bookings-calendar.tsx` (+ `.module.css`, client): `react-day-picker` v10 month grid **only**, custom `DayButton` colored by density (keyed off `CalendarDay.isoDate` so it matches the tz date keys), numeric count + descriptive `aria-label` per day (never color-only), today ring, past-day dimming, booking-horizon dimming (per-property horizon, or the furthest when viewing all), selected-day highlight, a density legend, and a property filter (All + each property). URL state `?month=YYYY-MM&property=<id|all>&day=YYYY-MM-DD` driven via `useRouter` (server stays the source of truth — month nav / filter / day select all re-fetch). **`<DaySchedule>` generalized** from `AdminBidListRow[]` to a shared `ScheduleBlock` interface (`{id, startTime, durationHours, guestName, guestCount, bookingType, href}`) + two adapters (`bidRowToScheduleBlock` for the dashboard's existing usage — kept working — and `bookingRowToScheduleBlock`, which links to `/admin/bids/<bidId>` when a bid exists else `/admin/bookings/<id>`); routing knowledge moved out of the component into the adapters. **Page rewrite** `app/admin/bookings/page.tsx` (server): parses month/property/day (defaults: current CT month, all properties, today — or the 1st when today is outside the viewed month), fetches the month + properties, computes density, renders `<BookingsCalendar>` above a per-property day-detail (`<DaySchedule>` per property in the dashboard `.columnGrid`, filtered to the selected day). Removed `<BookingFilters>` + `<BookingListTable>` (grepped — no other importers — and deleted the files); `app/admin/bookings/[id]/page.tsx` kept as the no-bid fallback detail. `tsc --noEmit` clean (build/dev gate runs on Node 20; this WSL shell has Node 18, so `next build` couldn't be smoke-tested here — manual browser walk recommended). Full plan: `plan/app/app-3-12-bookings-calendar.md`. |
| App 4 | Member Portal | 🔄 In Progress | **Sub-phase 4.0 landed 2026-05-18** — production `/login`, `<CyclingProperty>`, `<LoginAlert>`, brand design system in `app/globals.css`, `portalHomeForRole()` extracted to `lib/auth/portal.ts`, `/auth/callback` JWT refresh hardening (Google OAuth role-claim bug fix). Scenario F (10 steps) added to `docs/manual-testing.md`. **Plan drafted 2026-05-30** at `plan/app/app-4-member-portal.md` — three remaining sub-phases (4.1 my bookings, 4.2 adventures listing, 4.3 RSVP), dual-entry service contract (`getMyX(client)` + `getXForMember(client, scope)`) so App 3.8 preview-as-member reuses the same components. **Sub-phase 4.1 landed 2026-05-30** — household-visible bookings on `/member/bookings`. New `current_household_user_ids()` SECURITY DEFINER helper + `bookings: member household read` policy (replacing the prior `member_user_id = auth.uid()` policy) in `supabase/migrations/20260530120000_household_visible_bookings.sql`; Phase 7 RLS doc updated in the same commit (§4.2 helper table, §7.5 bookings policies, §10 changelog). New `<MemberNav>` tab strip mounted from `/member` and `/member/bookings`. New `<MyBookingsList>` + `<MyBookingCard>` pure presentational components — props in, JSX out, App-3.8-ready. Booking card shows date/time (property timezone), property name, booking type, status badge, guest count, instructor, price summary, "booked by X" attribution for spouse-booked rows, and bid status text on own rows (no link in v1 — public bid route needs the plaintext access code which the member doesn't have; member-authed bid view is future work). Service at `src/services/members/bookings.ts` (`getMyBookings`, `getBookingsForMember`); two-query strategy (bookings + people lookup) avoids needing an FK from `bookings.member_user_id → people.user_id`. `tsc --noEmit` clean (4 pre-existing `RouteHandlerConfig` errors in `.next/dev/types/validator.ts` predate this change). **Sub-phase 4.1 follow-ups landed 2026-05-30**: (a) **member_user_id stamping** — new migration `20260530140000_stamp_member_user_id_on_public_bookings.sql` adds `p_member_user_id` param to `create_public_booking` RPC and runs a one-shot backfill that links every prior NULL-attributed booking to its `people.user_id` via `guest_email`. Submit action at `app/(public)/book/[property]/submit/action.ts` now reads the auth session and stamps `member_user_id` + `audience_type='member'` when the caller is signed in as `member`. (b) **Funnel autofill** — `/book/[property]/layout.tsx` is now async, fetches the signed-in member's `people` row, and seeds the BookingFlowProvider with name + email prefill. Phone made optional everywhere (zod schemas, form UI label, `isSubmittable` narrow). (c) **Booking detail page** — new route `/member/bookings/[id]` with `BookingDetailView` composing summary / schedule notes / disciplines + add-ons / gear list / FAQ / pricing sections. Migration `20260530160000_household_visible_booking_children.sql` extends household visibility to `bids`, `booking_disciplines`, `booking_add_ons` (drops the `member read own` policies, recreates as `member household read` using the same `current_household_user_ids()` helper). Spouse can see full trip details on the other spouse's booking. Sign + pay surfaces unchanged — still routed via the email link with the access code. Service at `src/services/members/booking-detail.ts` with tolerant `gear_list`/`faq` jsonb parsing. List cards now wrap in `<Link>` to the detail page. Phase 7 RLS doc updated (§7.5/7.6/§10). **Manual verification pending** — Scenarios G1–G10 added to `docs/manual-testing.md`. **Pending migrations** (user to push): `20260530140000` (stamping + backfill) and `20260530160000` (household child-table RLS). **Downstream consumer**: App 3 sub-phase 3.8 (`/admin/members/[id]/preview`) re-renders these same dashboard components driven by admin-RLS-scoped data via the explicit-scope service variants. 3.8 is gated on App 4 completion. **Planned but not built**: sub-phase 4.5 (shareable trip link — post-finalization anonymous link to trip details minus cost/payment info). **Remaining App 4 work**: 4.2 adventures listing, 4.3 RSVP, 4.5 shareable trip link. |
| App 5 | Partner Portal | 🔲 Not Started | Concierge login, book on behalf of guest |
| App 6 | Payments | ✅ Done | Full Stripe deposit flow end-to-end (verified 2026-05-23 against `stripe listen` locally AND the production webhook endpoint on `rhytm-one.vercel.app`): PaymentIntent + `<PaymentElement>` with Path A variable-amount picker (customer pays anywhere in `[deposit, effective_quote]`, balance settles at the property), webhook handler with Phase 6 claim-first idempotency, branded receipt via the existing `EmailService` interface, admin manual refund (Stripe Refunds API + idempotent UPDATE flipping bid to `refunded` and booking to `cancelled` via the trigger). Workflow finalization rule: bid is "fully finalized" when `paid_at AND signed_at` both stamped — sign and pay are now independent (either ordering reaches `deposit_paid`). Status color taxonomy redesigned (amber → blue → olive → green active path; tan/red/gray terminals branched by reason). PaymentStatusBadge ("Paid in full / Deposit paid / Partial payment") on `/admin/bids` list + detail. Migrations: `20260523120000` (enum `+'refunded'`), `20260523120100` (`bids.paid_at` + `bookings.deposit_checkout_session_id` + `sync_booking_from_bid` relaxed + `refunded` arm), `20260523130000` (drops `deposit_checkout_session_id` after Pattern A pivot — see plan), `20260524120000` (`bookings.amount_paid`). Test pack S1–S15 in `docs/manual-testing.md`. Plan: `plan/app/app-6-stripe-deposit.md`. **Independent next pieces**: App 7 (Dropbox Sign — sign-after-pay is supported by the trigger; just needs the webhook + UI to stamp `signed_at`), App 8 (Resend transport — current receipts write to `dev_email_outbox`). |
| App 7 | E-Sign (homegrown waiver) | ✅ Done | **Reworked 2026-05-31 — replaced Dropbox Sign with an in-house typed-signature waiver** (branch `feature/homegrown-waiver`, 10 commits, verified end-to-end through the live UI; not yet merged to main). Guest flow: mobile-first `<dialog>` modal → type legal name + consent → Server Action renders a PDF (`pdf-lib`), stores it in a private Supabase Storage bucket (`waivers`), and records the signature atomically via the `record_bid_signature` RPC — synchronous, no webhook, no polling. `bids.signed_at` stays the canonical signal; the guarded `confirmed → signed` advance + `bid/signed` / `booking/confirmed` Inngest events reproduce the old onSigned contract verbatim. New tables: `waiver_templates` (config-in-DB, versioned, one active per property — admin editor at `/admin/settings/waivers` via `save_waiver_template` RPC) + `waiver_documents` (artifact + legal audit: SHA-256, frozen `signed_name`, IP/UA, `signer_user_id`). Admin **"View signed waiver →"** on the Lifecycle card streams the PDF via a 60s RLS-gated signed URL (`app/admin/bids/[id]/waiver/route.ts`). Migrations `20260531120000`–`20260531160000` (data layer + RPC, grant lockdown, storage bucket, RPC idempotency reorder, save-template RPC) — all applied live; RLS verified per rule #6. **Dropbox Sign path deprecated IN PLACE, not deleted** — code + deps (`@dropbox/sign`, `hellosign-embedded`) + `bids.dropbox_sign_envelope_id` retained; revive via `WAIVER_PROVIDER=dropbox_sign` (`lib/waiver/provider.ts` + `src/services/dropbox-sign/DEPRECATED.md`). **Deferred out of v1**: waiver re-issue/reset admin action, customer self-download route, member-read RLS on `waiver_documents`. **Pending**: merge `feature/homegrown-waiver` → main. |
| App 8 | Notifications | 🔄 In Progress | **Code complete 2026-05-24** — Resend SDK installed (`resend@6.12.3`); `ResendEmailService` class at `src/services/notifications/resend-email-service.ts` (same `EmailService` contract as `LoggingEmailService` — `@react-email/render` to HTML + `toPlainText()`, deterministic per-(source,template,recipient) `idempotencyKey`, structured error returns); factory in `src/services/notifications/send-email.ts` branches on `EMAIL_TRANSPORT === "resend"` (lazy-require keeps Resend SDK out of dev/test paths); `DEFAULT_FROM_EMAIL` reads `RESEND_FROM_EMAIL` with the `no-reply@rhythm.local` placeholder fallback. **Domain setup resolved without new DNS work**: client's existing Resend account already had `send.rhythm.co` verified (Netlify DNS records dated 2026-05-07 — DKIM + SPF + bounce-routing MX present). Pivoted to use that account instead of the new one I created (cleaner pattern — apex `rhythm.co` MX stays dedicated to Google Workspace inbound mail; `send.rhythm.co` is the isolated transactional reputation lane). API key (`re_a2rcu…`) provided by client, added to `.env.local`. End-to-end delivery verified 2026-05-24 — `POST /emails` returned `id 45733227-…` and the mail landed in the verified `jtc006@gmail.com` inbox from `Rhythm Outdoors <bookings@send.rhythm.co>`. Plan: `plan/app/app-8-resend.md`. **Activation blocked** on three client decisions: Q1 (from address — `bookings@` / `noreply@` / `hello@`), Q2 (display name — single "Rhythm Outdoors" vs per-property), Q3 (reply-to inbox). All three captured in `plan/questions/2026-05-24/app-8-resend/`. Once answered, Phase 4 = add `EMAIL_TRANSPORT=resend` + `RESEND_API_KEY` + `RESEND_FROM_EMAIL` + `RESEND_REPLY_TO` to Vercel and redeploy. **Optional next**: local end-to-end test through the EmailService factory (flip `EMAIL_TRANSPORT=resend` in `.env.local`, run a public booking, verify mail lands — the curl test proved API + delivery; this proves the factory + render path). Test pack E1–E6 deferred to post-activation. |
| App 9 | Workflows | 🔄 In Progress | **Sub-phase 9.1 (scaffold) landed 2026-05-24.** `inngest@4.5.0` installed; client at `lib/inngest/client.ts` (id `rhythm-outdoors`); typed event registry at `lib/inngest/events.ts` using v4 `eventType()` + `staticSchema()` (note: v3's `EventSchemas.fromRecord` is removed in v4 — the 2026-05-24 scaffold actually shipped against the v4 surface, not v3 as originally noted) covering bid lifecycle (`bid/created`, `bid/confirmed`, `bid/signed`, `bid/deposit-paid`, `bid/expired`), booking lifecycle (`booking/confirmed`), and membership lifecycle (`membership/application-submitted`, `membership/approved`); one scaffolded no-op handler at `lib/inngest/functions/on-bid-created.ts` subscribed to `bid/created` (proves wiring); function barrel at `lib/inngest/functions/index.ts`; serve handler at `app/api/inngest/route.ts` exposing GET/POST/PUT via `inngest/next`. **2026-05-30 audit fixes**: `.env.example` + `lib/inngest/client.ts` comments + plan doc now document that Inngest 4.x dropped NODE_ENV-based dev-mode inference and `INNGEST_DEV=1` in `.env.local` is required for local dev; `import "server-only"` added to `lib/inngest/events.ts`. Plan: `plan/app/app-9-inngest.md`. **Sub-phase 9.2 — event firing wiring** ✅ **Done 2026-05-30.** All five events fire after DB commits: `bid/created` from `create-public-booking.ts`, `bid/confirmed` from admin `confirmBid` Server Action (threads `staffId` from session), `bid/signed` from Dropbox Sign webhook (only when stamp actually affected a row — idempotent against replays), `bid/deposit-paid` from Stripe webhook (only when bid flip succeeded), `booking/confirmed` from BOTH webhooks at the finalization convergence point (sign-then-pay fires from Stripe handler when `updatedBid.signed_at !== null`; pay-then-sign + waiver-only fire from Dropbox Sign handler keyed off `requiresDeposit ? paid_at !== null : true` — mirrors `bid-timeline.tsx`'s finalization rule). All sends use stable producer-side dedupe ids (`bid-${bidId}-<verb>`, `booking-${bookingId}-confirmed` shared across webhook paths) and are best-effort via `after()` with logged failure (claim-first idempotency makes "let provider retry cover us" pointless; DB state is source of truth). Cloud Inngest verified end-to-end on Vercel for all three convergence paths: sign-then-pay, pay-then-sign, waiver-only (no deposit). **Sub-phase 9.3 — W5 confirmation email migration** ✅ **Done 2026-05-30.** Guest "we're preparing your bid" confirmation email moved out of `create-public-booking.ts`'s inline `after()` send and into a dedicated Inngest function `lib/inngest/functions/send-bid-confirmation-email.ts` subscribing to `bid/created`. Function has two cached steps — `lookup-booking-details` (single `bookings → properties` join for guest name, start time, property name + timezone) and `send` (render + transport via the existing `getEmailService()` factory). Send step throws on `!result.ok` so Inngest retries transport failures (3× + backoff) instead of swallowing them with a `console.error`. Event payload extended with one new field `bidPath` (relative `/bids/<slug>/<code>` URL with the one-time plaintext access code embedded — the DB only stores the bcrypt hash, so this is the single moment the code can reach downstream consumers; smaller surface than adding `bidSlug` + `bidAccessCode` as separate fields). `create-public-booking.ts` lost its `sendConfirmationEmailBestEffort` helper, `ConfirmationEmailArgs` interface, and 7 unused imports (`createElement`, `getEmailService`, `DEFAULT_FROM_EMAIL`, `getSiteOrigin`, `formatDateLong`, `formatSlotLabel`, `GuestBookingConfirmation`, `buildAbsoluteBidUrl`); kept `buildBidUrl` for return value + the new event payload. Atomic migration — new function + inline-send removal landed together, no double-send window. Verified locally 2026-05-30: bid/created event fires → both `scaffold-on-bid-created` AND `send-bid-confirmation-email` runs land COMPLETED in the Inngest dashboard → exactly one row in `dev_email_outbox` (no duplicate from the old inline path). **Sub-phases 9.4+ (each blocked on specific Q answer)**: bid auto-expiry (Q7), 48h unsigned follow-up (Q7 sub-question), pre-event email cadence (Q15), membership approval routing (Q8), HubSpot deal sync (client account access + pipeline shape). |
| App 10 | Observability | ⏸ Deferred to pre-1.0 launch | Sentry error tracking + Axiom logs/events. Plan drafted 2026-05-30 at `plan/app/app-10-observability.md` — five sub-phases (10.1 Sentry baseline → 10.2 Sentry depth → 10.3 Axiom log drain → 10.4 Axiom events + dashboards → 10.5 alerts). **Deliberately deferred 2026-05-30** (not client-blocked). Pre-launch, errors are findable by grepping Vercel logs; observability compounds with real traffic + ongoing dev. Sub-phases 10.4 (event vocabulary) and 10.5 (alert thresholds) are feature-coupled and would churn if client feedback reshapes the funnel. Pick this up immediately before 1.0 launch — that's the moment Sentry + Axiom start paying for themselves. |
| App 13 | Bid Content Library | 🔲 Not Started | **Reusable FAQ + gear-list templates that auto-fill new bids by property / discipline / booking type.** Plan drafted 2026-05-31 at `plans/bid-content-library.md` (includes realistic placeholder seed data). Removes hand-writing the same FAQ/gear on every bid of the same kind. Two parallel SOLID-purist table pairs — `bid_faq_templates` + `bid_faq_template_scopes`, `bid_gear_templates` + `bid_gear_template_scopes` — each scope row tagging a `global` / `property` / `service` / `booking_type` axis. A `resolve_bid_content(property_id, service_ids[], booking_type)` SQL function unions matches and **dedupes by `dedupe_key`** with precedence `service > booking_type > property > global` (lets a property-specific item override a global one). Called inside `create_public_booking()` to auto-fill `bids.faq` / `bids.gear_list` at creation (stays atomic), and via RPC for an editor "re-pull". Snapshot model preserved — editing a template never mutates an already-composed bid. Staff-only RLS, no cross-table subqueries (no cycle risk). Admin CRUD at `/admin/templates`; editor gains Add-from-library + Re-pull. Build order + seed content in the plan doc. Pairs with the Phase 3 deferred `jsonb_typeof` CHECK on `bids.gear_list` / `bids.faq`. |
| App 12 | Guest Waivers | 🔲 Optional / Not Started | **Optional enhancement — not on the critical path.** Lets the primary signer collect waivers from the rest of their party. Plan drafted 2026-05-30 at `plan/app/app-12-guest-waivers.md` — six phases (1 schema + DB functions → 2 service layer → 3 webhook metadata routing → 4 public `/waiver/[token]` page → 5 bid-page red/green grid → 6 email invite + admin visibility). New `bid_guest_waivers` table, one Dropbox Sign envelope per guest (reuses the App 7 template, created lazily on link-open), per-guest bcrypt token. **Non-blocking by design:** booking still finalizes on primary sign + deposit; guest waivers are a parallel completeness track and never touch `bids.status` or `bookingConfirmed`. Open question before build: minor/guardian co-signing (cheap to add in phase 1/4, painful to retrofit). |

---

## Open Questions (blocking seed data or design decisions)

| ID | Question | Blocking |
|----|----------|---------|
| Q2 | Operating hours and instructor headcount per property | time_slots seed, instructors seed |
| Q4 | Full discipline + add-on catalog for Hog Heaven and Packsaddle | services seed, add_ons seed |
| Q5 | Group pricing tiers for plan_a_visit (per-person rates by group size) | pricing_rules seed |
| Q9 | Membership tier names and definitions | members.membership_tier, adventures access gating |
| Q14 | Adventures: deposit upfront or full payment at RSVP? | Phase 5 payment flow |
| Q16 | Annual dues model (affects membership lapse logic) | membership_status_enum |

---

## Deferred Improvements

Items identified during review as worth doing eventually but explicitly deferred — not blocking, not bugs. Promote to active work when the listed condition becomes true.

| Phase | Item | Reason Deferred | Promote When |
|-------|------|----------------|--------------|
| Phase 3 | `generate_bid_slug` retry-on-conflict inside the function | Slug collisions resolve via UNIQUE constraint + app-layer retry; expected volume is tiny | Logs show serialization errors at bid creation |
| Phase 3 | `bids_status_history` audit trail (who confirmed/denied/regenerated, when) | Single-staff operation today; cheap to add later but adds a table + trigger | More than one staff member regularly edits bids, or a dispute requires reconstructing a bid's history |
| Phase 3, 5 | `CHECK (jsonb_typeof(...))` on `bids.gear_list`, `bids.faq`, and `member_adventures.details` | App layer validates shape today; small trusted authoring team | Before public launch, or after any bug from malformed JSON |
| Phase 5 | `created_by_user_id` audit on `member_adventures` and `member_adventure_rsvps` | Same audit-trail family as Phase 3 bids — single-staff operation today | Dispute resolution requires "who created/edited this adventure or RSVP" or more than one staff member regularly touches adventures |
| Phase 5 | Belt-and-braces "RSVP's member.property_id matches adventure.property_id" trigger | RLS already enforces this on the member path; service-role mis-writes would be the only way to violate it | A bug or incident shows a service-role path could create a mismatched RSVP |
| Phase 5 → 6 | Waitlist promotion via Supabase Database Webhook → Inngest depends on the Phase 6 webhook/idempotency layer | Configurable in dashboard (no SQL), but the receiving Inngest endpoint must use `processed_webhooks` for dedupe | When Phase 6 is built — wire the `rsvp.cancelled` webhook through Phase 6's idempotent receiver |
| Phase 6 → 10 | Structured logging on the duplicate-claim path (`23505` conflict) for idempotency observability | Belongs in Phase 10 (Sentry / Axiom integration), not the webhook handler itself | Phase 10 is in progress — emit a `webhook.duplicate_claim` structured event with `{ source, event_type, id }` so Axiom can chart it |
| App 2 → pre-launch | Promote service-role public reads (slots availability + pricing rules) to `SECURITY DEFINER` RPCs | App 2 placeholder mode — `bookings` and `pricing_rules` have no anon SELECT RLS, so the public funnel reads them via service-role. Service-role works but enlarges the trusted surface. RPCs (`public_available_slots(...)` and `public_pricing_for_booking(...)`) encapsulate the queries and let anon call them via `SET search_path = public` + careful return shapes — column-projected, PII-free. Today's callers: `src/services/public/slots.ts`, `src/services/public/pricing.ts`. | Before public launch, OR before adding a third service-role read caller |
| Cross-cutting | Extend the intent-revealing-naming sweep (2026-05-22) to the rest of the codebase | First pass covered admin pages + filters + admin services + bid services + memberships service + dev actions + login form + booking date helpers (17 files). Banned: `raw`, `q` (var), `qs`, `v`, `obj`, `fmt`, single-letter map vars. Kept idioms: `i`, `e`, `err`, `ctx`, `s` (CSS), `prev`/`next`. Rules live in `.claude/skills/naming/SKILL.md` (auto-triggers on TS/TSX edits) + memory `feedback_intent_revealing_names.md`. Untouched: most of `src/components/public/booking-flow/*`, `app/(public)/book/**`, public services beyond `bids/get-bid.ts`. | Naturally — apply the rules whenever a file in those areas is next touched. No need to do as a standalone PR. |
